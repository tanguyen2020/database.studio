/** Injected by Vite (`define` in vite.config.ts) from package.json — the running
 *  app version, shown in Settings → Updates and compared against the release the
 *  updater finds. */
declare const __APP_VERSION__: string
