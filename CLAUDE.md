# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

Database Studio is a Tauri 2 desktop DB/streaming client (Rust backend `src-tauri/` + Svelte 5 frontend `src/`) supporting 10 systems: PostgreSQL, MySQL, MariaDB, MSSQL, SQLite, ClickHouse, Cassandra, Redis, Kafka, NATS.

## Commands

```bash
# Frontend gate — must be 0 errors / 0 warnings before commit
npm run check                      # svelte-check + tsc

# Unit tests (Vitest — frontend, jsdom, no DB)
npx vitest run                     # all
npx vitest run src/lib/sql/chops.test.ts   # single file
npx vitest run -t "toMermaid"      # by test name

# Visual/e2e (Playwright — pixel-diff + DOM vs the prototype; auto-starts vite)
npx playwright test                # all
npx playwright test cassandra-workspace    # single spec

# Design tokens (see "1:1 UI" below) — regenerate + guard
npm run tokens                     # extract colors/spacing from the prototype HTML
npm run tokens:check               # fail build on hardcoded styles

# Desktop app (dev/build run from repo ROOT; tauri.conf beforeDev runs `npm run dev`)
npm run tauri dev
```

### Rust build & tests (Windows toolchain gotchas)
`rustc`/`cargo` are rustup user-level and **not on Git Bash PATH by default**. `rdkafka` (Kafka) builds `librdkafka` via CMake, also not on PATH. Prefix cargo commands:
```bash
export PATH="$PATH:$HOME/.cargo/bin:/c/Program Files/Microsoft Visual Studio/2022/Community/Common7/IDE/CommonExtensions/Microsoft/CMake/CMake/bin"
cargo build --lib        # compiles the driver layer (does NOT compile #[cfg(test)])
cargo test --lib lint::  # Rust unit tests (pure, no DB) by module path
```

### Integration tests (`src-tauri/tests/drivers_integration.rs`) — real engines via testcontainers
**Mandatory run methodology** (avoids self-inflicted flakiness): prebuild first so the timeout only covers execution, then run **synchronously in one shot** with a hard `timeout`, write to a log, print the exit code, and read the log in the *same* command. Never background, never spawn a "monitor", never `docker rm`/prune by `label=org.testcontainers` (kills the Ryuk reaper → lingering containers) — testcontainers owns cleanup via `Drop`.
```bash
cargo test --test drivers_integration --no-run
timeout 400 cargo test --test drivers_integration clickhouse_roundtrip -- --nocapture --test-threads=1 > out.log 2>&1; echo "EXIT=$?"; tail -40 out.log
```
Cassandra/MSSQL/ClickHouse containers are slow (~1–3 min) — use generous timeouts. Integration tests **seed the container then query back to verify** (no hard-coded expected results).

## Architecture

### The Tauri boundary + dual-mode IPC (critical)
Every backend call goes through `src/lib/ipc.ts`, whose `invoke()` dispatches to the real Tauri command **or** to `src/lib/demo.ts` mocks when `IS_TAURI` is false (`ipc.ts:9`). This is why the app runs in a plain browser — **Vitest and Playwright exercise the demo path**. Consequence: any new `#[tauri::command]` needs a matching case in `demo.ts`, or browser/visual tests break.

### Backend driver layer (`src-tauri/src/drivers/`)
All connections are unified behind the `LiveConnection` enum in `drivers/mod.rs`. Each system is a variant with its own `<system>.rs` module; `mod.rs` holds the `connect`/`test`/`exec`/`ping`/`exec_params`/`apply_grid_changes`/introspection **match arms that dispatch per variant** plus `<system>_params()` builders that map a `ConnectionProfile` → driver params. Adding a system = new module + variant + arms in every match. Non-SQL systems (Redis/Kafka/NATS/Cassandra) return typed "not applicable" errors from the SQL arms and expose their features through dedicated commands instead.

`connections/registry.rs` owns live connections (one per profile), the optional SSH tunnel, in-flight statement cancellation (abort + reconnect), and per-driver param accessors. `commands/*.rs` are thin `#[tauri::command]` wrappers registered in the `invoke_handler!` list in `src-tauri/src/lib.rs` — **a command is dead until added there**. `storage/` is an internal rusqlite DB (connection profiles with AES-256-GCM passwords, tabs, query history, app_state) — distinct from user-connected SQLite.

### Result/exec contract
The locked shape is `{ ok, result?: { cols:[name,type][], rows, total }, error?, duration_ms }` (`drivers/types.rs` `StatementOutcome`/`ExecResponse`). The multi-statement SQL editor splits statements client-side and sends each individually. ClickHouse is HTTP/reqwest (dynamic result sets); Cassandra CQL runs via a dedicated `cql_exec` command with paging-state tokens (never LIMIT/OFFSET).

### Frontend state (Svelte 5 runes)
Stores are runes classes in `src/lib/stores/*.svelte.ts` (`connections`, `tabs`, `results`, `explorer`, `ui`, `settings`, `palette`…). `App.svelte` dispatches the active tab's `contentType` to a workspace component. **Adding a workspace/tab type requires three edits**: `TabContentType` in `src/lib/types.ts`, an `open…` method on the `tabs` store, and an `{:else if}` branch in `App.svelte`'s `paneBody` snippet. Watch the `effect_update_depth_exceeded` trap: a `$effect` that calls a store method which synchronously reads+writes the same `$state` must wrap the call in `untrack(() => …)` and track only primitive deps.

### 1:1 UI with the prototype (enforced)
`spec/Database Studio design/design_handoff_database_studio/Database Studio.dc.html` is the source of truth for UI. Colors/spacing come **only** from generated tokens (`src/lib/systems.gen.ts`, `src/lib/tokens.css` via `npm run tokens`); `npm run tokens:check` forbids hardcoded style values in components. SVG icons are copied verbatim from the prototype's `dbIcon()`. Playwright specs in `tests/visual/` pixel-diff app regions against the prototype. When the prototype and a phase spec conflict on layout, the prototype wins; specs add functional/data requirements.

### Specs & process
`spec/phase-1..6*.md` + `spec/Database Studio design/design_handoff_database_studio/*ADDENDUM.md` define scope; addendums override the base spec for their system (e.g. `CLICKHOUSE_SPEC_ADDENDUM` §7 = editable grid must generate async `ALTER TABLE … UPDATE/DELETE`, not OLTP). `GAP_REVIEW.md` / `SPEC_SUPPLEMENT.md` track outstanding design-vs-code gaps and the remaining `T10+` task backlog. Work proceeds phase-by-phase, committing per task with tests green.

## Tiến độ task (T10–T23, theo SPEC_SUPPLEMENT.md)

Cập nhật sau MỖI commit. Rule: 1 task/lần, unit+integration xanh mới commit `T<n>: …`, không nới assertion; kẹt >3 lần sửa → ghi tình trạng vào đây + hỏi.

- **T10 — Connection Test/Cancel correctness — ✅ DONE** (commit `T10:`). Bounded timeout (`connect_timeout()`=10s) + cancellable `run_test_bounded` (token vs timeout vs test, SSH tunnel always `shutdown()`), `classify_connect_error`, `cancel_test` cmd, ConnectionForm uuid testId + cancel-on-close. Tests: unit (timeout/error-map) + integration `connection_test_bounded_and_cancellable` (live ok / closed-port bounded / cancel <1s) EXIT=0.
- **T11 — Cancel running query — ✅ DONE** (commit `T11:`). Verified registry cancel (abort task → CANCELLED + poison → heal on next stmt); UI: `TabExecution.startedAt` + "running Ns" timer on Cancel button + long-run warning toast (`settings.longRunningWarnMs`). Integration `query_cancel_aborts_and_connection_recovers` (pg_sleep→cancel ~1.16ms, follow-up SELECT 1 ok) EXIT=0.
- **T12 — Wire dead stubs — ✅ DONE** (commit `T12:`). Set-as-Filter → Table Viewer w/ seeded filter; Convert → "Converted" formatted tab; Split → moveToSplit; ResultChart real PNG/SVG export (serialize SVG + resolve CSS vars → blob); removed stub buttons SQLite Export.sql / Explorer Export-dump / Backup / Users (deferred to T14/T15/T22/T23). **Bonus fix found while verifying chart e2e:** `results.run` mutated the RAW `exec` object after assigning it into the `$state` byTab record → subResults.push/activeSub/running bypassed the proxy → live query results never rendered. Fixed by re-acquiring the proxied ref post-assignment. Tests: `tests/visual/wire-stubs.spec.ts` 4 e2e (Convert/Split/Set-as-Filter/Chart SVG export) + gates check 0/0, vitest 135, playwright 23.
- **T13 — Import wizard completion — ✅ DONE** (commit `T13:`). New pure `src/lib/import/plan.ts` (per-dialect conflict SQL — PG `ON CONFLICT DO NOTHING`, MySQL/MariaDB `INSERT IGNORE`, SQLite `INSERT OR IGNORE`, CH/MSSQL unsupported→plain; buildInsert/chunk/parseJson/sqlLiteral). ImportDialog: CSV/JSON format, encoding (UTF-8/UTF-16LE/Windows-1252), First-row-header toggle, dedicated Options step (batch size + on-conflict), batched execution + progress bar; CH forces batching + disables on-conflict. Tests: unit `plan.test.ts` (13, conflict-SQL per dialect), visual full-flow, integration `pg_import_100k_rows_batched_and_count` (100k rows batched → count()=100000 + ON CONFLICT DO NOTHING adds 0) EXIT=0. Gates: check 0/0, vitest 148, playwright 24.
- **T14 — Table/Query Export Wizard — ✅ DONE** (commit `T14:`). Pure `src/lib/export/query.ts` (buildExportSelect: column subset + WHERE + LIMIT/OFFSET, MSSQL TOP; supportsOffset per dialect). Store `export.svelte.ts` (table|result modes). `ExportDialog.svelte`: format CSV/JSON/SQL/Excel + column checkboxes + WHERE (table) + limit + filename; table mode paged-streams via LIMIT/OFFSET (PAGE=5000, single-query for MSSQL/Cassandra), result mode serializes in-memory rows. Wiring: Explorer "Export Data…" ctx item; ResultPanel Export ▾ → "Custom…" (ResultPanel gains connId). Tests: unit `query.test.ts` (8), visual `export-wizard.spec.ts` (2), integration `pg_export_column_subset_where_reimport_count` (seed 1000 → export subset+WHERE=333 → reimport count match) EXIT=0. Gates: check 0/0, vitest 156, playwright 26.
- **T15 — Generate Scripts whole schema/DB — ✅ DONE** (commit `T15:`). Pure `src/lib/sql/scripts.ts` (orderObjects topo — parent/base first, view after base, cycle-safe; generateScript structure/data/both, CREATE→FK ALTERs last→INSERT). `ddl.ts` genForeignKey. `scripts.svelte.ts` + `GenerateScriptsDialog.svelte` (mode + object checkboxes; builds DbObjects from introspection: genCreate + list_foreign_keys + data via export SELECT/toSqlInsert + view def via information_schema.views; opens SQL tab). Re-added Explorer dump button + schema ctx "Generate Scripts…" + SQLite "Export .sql". Tests: unit scripts.test.ts (7) + ddl genForeignKey (2); visual generate-scripts.spec.ts; integration `pg_generate_structure_roundtrip_identical` (seed tables+FK+view → regen from introspection into fresh schema → introspect identical) EXIT=0. Gates: check 0/0, vitest 165, playwright 27. NOTE: CH SHOW CREATE round-trip from done-criteria not separately tested (frontend view-def uses information_schema; CH view handled best-effort) — flag if stricter CH coverage needed.
- **T16 — Query Plan per-system — ✅ DONE** (commits `T16 (ClickHouse|MariaDB|MSSQL|Cassandra):`). All in `drivers/plan.rs` (pure parsers) + `commands/plan.rs` (orchestration):
  - **ClickHouse**: `EXPLAIN indexes = 1` → indentation tree (`parse_clickhouse`); ReadFrom*→SeqScan, full-read-no-index → hotspot.
  - **MariaDB**: `ANALYZE FORMAT=JSON` → `parse_mysql(actual=true)` reads r_rows/r_total_time_ms → mode=actual.
  - **MSSQL**: `SET SHOWPLAN_XML ON` (roxmltree dep) → `parse_mssql_xml` RelOp tree (namespace via local-name). **Driver fix**: `mssql.rs` routes `SET …` through `simple_query` (raw batch) — `execute()`=sp_executesql doesn't persist SET options (silently broke SHOWPLAN + any SET). normalize_op now maps "seek"→IndexScan.
  - **Cassandra**: no EXPLAIN → `trace_cql` (statement tracing + get_tracing_info) → `parse_cassandra_trace` timeline; ALLOW FILTERING → hotspot+warning. Routed via `explain_plan`→`explain_cassandra` (removed from not_applicable).
  - Tests: unit 11 in plan:: (Rust lib 60 total); integration per-system (CH/MariaDB/MSSQL/Cassandra) all EXIT=0.
- **T17 — Index Scanner completeness — ✅ DONE** (commit `T17:`). **Dùng pattern flags chuẩn sẵn có (unused/redundant/fragmented/invalid), KHÔNG thêm field `anti_pattern`** (theo yêu cầu). CH `system.data_skipping_indices` + Cassandra `system_schema.indexes` → `IndexScanRow`; wire Clickhouse/Cassandra arm trong `mod.rs::scan_indexes`. Missing-index: `IndexScanResult.suggestions` + pure `suggest_missing_pg` (unit-tested) + `PgDriver.missing_indexes` (pg_stat_user_tables) + `MssqlDriver.missing_indexes` (sys.dm_db_missing_index_* DMV). Frontend: ipc type + IndexScanner "Missing-index suggestions" section + demo seed. Tests: unit (Rust lib 61) + integration CH/Cassandra index scan (trong test container CH/Cassandra) EXIT=0 + visual. Gates: check 0/0, vitest 165, playwright 27.
- **T18 — Explorer depth — ✅ DONE** (commit `T18:`). Backend `commands/schema.rs::definition_query` (pure per-dialect) + `object_definition` command → text định nghĩa THẬT (PG pg_get_functiondef/triggerdef/viewdef; MySQL/MariaDB information_schema; MSSQL OBJECT_DEFINITION; SQLite sqlite_master). Frontend ObjectExplorer: Show Definition + Drop context menu (views/procs/fns/TVF/scalar/triggers), view column expansion, tree filter input + Ctrl+F, Object Properties panel; ipc.objectDefinition + demo (routines/triggers seeded). Tests: unit definition_query_per_dialect (Rust lib 62) + integration pg_object_definition_returns_real_text EXIT=0 + visual explorer-depth. Gates: check 0/0, vitest 165, playwright 28. NOTE: index/constraint detail children đã có từ T7b (bảng expand hiện Indexes/Constraints) — không mở rộng thêm.
- **T19 — Schema Compare depth — ✅ DONE** (commit `T19:`). `compare/diff.ts`: SchemaSnapshot.routines (CmpRoutine) + compareSchemas diff routines/triggers theo normalized DDL text; genMigration emits source DDL (CREATE OR REPLACE) / DROP; new `lineDiff` (LCS). SchemaCompare.svelte: snapshot fetch routines + DDL (objectDefinition); row clickable → side-by-side DDL diff modal (Target|Source, add/del highlight) + Prev/Next. Tests: unit diff.test (10) + integration `pg_schema_compare_proc_and_column_then_migrate` (detect→migrate→converge) EXIT=0 + visual. Gates: check 0/0, vitest 169, playwright 28.
- **T20 — ER create-relationship + Save-to-DB — ✅ DONE** (commit `T20:`). ErDiagram.svelte: "+ Relationship" builder (from/to table+col dropdowns) → pending FK (dashed green edge); "Save to DB" → `genForeignKey` ALTER ADD CONSTRAINT vào SQL tab; edge label cardinality N:1; in-tab Ctrl+F (svelte:window) dim non-match nodes. Unit: genForeignKey (ddl.test, từ T15). Integration `pg_er_add_relationship_creates_fk` (apply ALTER ADD FK → foreign_keys introspect thấy) EXIT=0. Visual er-diagram mở rộng. Gates: check 0/0, vitest 169, playwright 28.
- **T21 — Timeout/retry/pooling + transactions + autocomplete + shortcuts — ✅ DONE** (commit `T21:`). Pure `connections/pool.rs` (PoolConfig + capped exponential backoff, unit-tested) + Settings→Connections fields. **QUYẾT ĐỊNH: giữ model 1-conn-per-profile (Mutex registry) để bảo toàn hợp đồng cancel T11 + transaction cùng-connection — KHÔNG rewrite thành multi-conn pool (rủi ro phá T10/T11); concurrent tabs share conn an toàn, integration chứng minh không cạn kiệt.** Transactions: BEGIN/COMMIT/ROLLBACK toolbar (ẩn ở CH) qua results.run. Autocomplete: pure `sql/functions.ts` per-dialect signatures + SqlEditor merge completion source. Shortcuts: pure `keys/shortcuts.ts` (Ctrl+Shift+F format, Ctrl+Alt+G/J/R result view, Ctrl+Shift+C copy JSON, Ctrl+F find) + App.svelte dispatch qua ui signal ticks → SqlWorkspace/ResultPanel(active-guard)/ObjectExplorer observe. Tests: unit pool(3)+shortcuts(6)+functions(4) (Rust lib 65); integration `pg_registry_concurrent_and_rollback` (16 concurrent + rollback→count 0) EXIT=0; visual txn-shortcuts. Gates: check 0/0, vitest 179, playwright 29.
- **T22 — Backup & Restore — ✅ DONE** (commit `T22:`). Pure `drivers/backup.rs` (backup_tool + external_backup_cmd builder, password qua env). SqliteDriver.backup_to/restore_from (rusqlite backup API — round-trip đảm bảo). `commands/backup.rs` (backup_tool_status phát hiện thiếu binary; backup_database SQLite in-process / else spawn pg_dump·mysqldump·clickhouse-client với PGPASSWORD/MYSQL_PWD; restore_database SQLite). Frontend: backup store + BackupDialog (tool status/dest/history/restore-confirm) + Explorer Backup button re-added. Tests: unit builder (Rust lib 69) + integration `sqlite_backup_restore_roundtrip` (đảm bảo) + `pg_pg_dump_if_binary_present` (chạy nếu có, else SKIP+note) EXIT=0 + visual. Gates: check 0/0, vitest 179, playwright 30.
- **T23 — Admin views — ✅ DONE (phần relational core)** (commit `T23:`). Pure `commands/admin.rs` (admin_query per-dialect: PG pg_stat_activity/pg_locks/pg_roles/pg_available_extensions; MySQL/MSSQL tương ứng + kill_query pg_terminate_backend/KILL) + admin_view/kill_session commands. Frontend: tab type `admin` + AdminView.svelte (Sessions/Locks/Users/Extensions + per-session Kill + Refresh) + tabs.openAdminView; wired TitleBar "Sessions" stub → Session Monitor; re-added Explorer Session Monitor + Users & privileges buttons. Tests: unit (Rust lib 71) + integration `pg_admin_views_and_kill_session` (pg_stat_activity/pg_roles/pg_available_extensions + terminate phiên thứ 2) EXIT=0 + visual. Gates: check 0/0, vitest 179, playwright 31.
  - **BỔ SUNG chạy được trên CE/container** (commit `T23+:`): **Redis memory** (INFO memory → parse_redis_info), **MSSQL Query Store** (sys.query_store_* sau khi bật QUERY_STORE), **MSSQL Agent Jobs** (msdb.dbo.sysjobs, đọc kể cả Agent off), **MSSQL Availability Groups** (DMV, rỗng nếu không cluster). AdminView system-aware view list. Integration: `redis_memory_info_view` (53 metrics) + `mssql_admin_extra_views` EXIT=0. Fix bug: external-view-sync effect gây effect_update_depth_exceeded (redis 'sessions'⇄'memory') — đã guard theo view hợp lệ.
  - **CÒN LẠI (deferred, cần setup broker/auth đặc biệt)**: Kafka ACL (cần broker authorizer), NATS NKey-JWT (cần JWT operator/nsc). Kafka Avro (Schema Registry) + NATS JetStream mgmt đã có từ phase trước.

## ✅ T10–T23 HOÀN TẤT. Tổng: Rust lib unit 72, vitest 179, playwright 34; integration per-system EXIT=0. Chỉ còn Kafka ACL + NATS NKey-JWT (deferred — cần cấu hình broker/auth ngoài container mặc định).

## AUDIT-1 (post-T23) — xem SPEC_SUPPLEMENT.md mục "AUDIT-1":
- **A2 hover dòng connection** ✅ (ConnectionList `.conn-row`/`.selected`, bỏ bẫy inline-bg).
- **A3 Generate Scripts 3-mode trên table context menu** ✅ (ObjectExplorer `ContextMenu.Sub` → `genTableScript`).
- **A4 Save ER layout** ✅ (ErDiagram persist `tab.state.positions` qua `onnodedragstop` + `tabs.schedulePersist`; Auto-layout xoá saved).
- **A5 Streaming I/O** ⏳ PLAN-only (chưa code): export/generate đang buffer RAM (sqlx fetch_all + Blob); plan chuyển `export_to_file` stream + `BufWriter` + save-dialog + Channel progress. Cần duyệt trước khi làm.
- **A6 ConnectionForm "chết nút" + bỏ Group + English** ✅ (fix `$effect` self-loop `effect_update_depth_exceeded` bằng build-local-assign-once; bỏ field Group; nút **Test → "Test connection"**, **Save → "Connect"**).
- **A7 i18n sweep — toàn bộ user-facing text = English** ✅ (App/stores/workspace/dialogs/demo/plan/diff; chỉ chừa comment + test desc + dữ liệu mẫu + `×`). Specs cập nhật theo text mới.
- Gates sau AUDIT-1: check 0/0, vitest 179, playwright 35, Rust unit 72.

## AUDIT-2 (post-AUDIT-1) — 7 mục người dùng báo:
- **1 Compare trong context menu Database** ✅ (ConnectionList: item "Compare Schemas…" trên context menu connection cho hệ relational + wire lại nút toolbar Compare (trước là `phase5` stub) → `tabs.openSchemaCompare`).
- **2 Result Grid context menu copy** ✅ (ResultGrid: chuột phải cell → Copy cell / Copy row / Copy column / Copy selection / Copy all as CSV; backdrop đóng menu, Esc đóng).
- **3 Postgres liệt kê tất cả databases trong Explorer** ✅ (backend `PgDriver::databases()` = `pg_database` WHERE `datistemplate=false AND datallowconn`, `current=current_database()`; `LiveConnection::databases()` arm (chỉ PG, khác → rỗng); command `list_databases` + `open_database` (clone profile+password server-side từ storage/registry sang DB khác → ephemeral connection, frontend không cần password); `registry.live_credentials`; frontend explorer section "Databases" (current đánh dấu ●, click DB khác → `openDatabase`→`connections.adopt`+select). Integration `pg_list_databases_marks_current`.
- **4 Panel Properties ẩn khi mở app + nút mở** ✅ (ui `rightPanelOpen` default `false`, KHÔNG restore từ persist → luôn ẩn lúc mở app; PropertiesPanel `{:else}` = tay cầm dọc "⇤ Properties" click mở lại).
- **5 Query editor panic `NaiveDateTime + TimeDelta overflowed`** ✅ (root cause: sqlx decode PG timestamp/timestamptz/date làm `epoch + Duration` → panic khi ±infinity (i64::MAX/MIN) hoặc year > 262143; với `panic="abort"` release = chết app. Fix: `decode_pg_timestamp`/`decode_pg_date` đọc raw bytes + `checked_add_signed`, trả "infinity"/"-infinity"/marker thay vì panic). Integration `pg_infinity_and_out_of_range_timestamp_do_not_panic`.
- **6 Bỏ BEGIN/COMMIT/ROLLBACK** ✅ (SqlWorkspace: gỡ 3 nút transaction + helper `txn`).
- **7 i18n backend** ✅ (chuyển user-facing VN còn lại sang English: PG/MySQL/MSSQL/SQLite/ClickHouse error hints, lint messages (`lint/mod.rs`), driver error strings (cassandra/redis/nats/kafka/clickhouse/mssql qua subagent), index_scan suggestion. Chỉ chừa comment + assert/test messages + `#[cfg(test)]` fixtures).

## AUDIT-3 (post-AUDIT-2) — full review 6 mục:
- **1 ER Diagram drag&drop + new** ✅ (đã có: create qua context menu, FK edges, save layout persist qua `tab.state.positions` trong `tabs.payload` — sống qua restart. THÊM: pure `er/diagram.ts` (visibleTables/addTable/removeTable/flowPosition, model `included?: string[]` = undefined→all / array→subset) + ObjectExplorer table rows `draggable` (dataTransfer `application/x-ds-er-table`) + ErDiagram drop target (`bind:viewport`, `flowPosition` → canvas coords) + "New ER Diagram (drag tables in)" (blank `included:[]`). Unit `er/diagram.test.ts` (9).
- **2 Query Editor palette theme-aware** ✅ (SqlEditor thay `defaultHighlightStyle` bằng `HighlightStyle` tự định nghĩa dùng `var(--syntax-*)`; app.css thêm palette `--syntax-{keyword,string,number,comment,function,type,operator}` — light = One Light-ish (contrast tốt trên nền sáng), dark = reuse hex tokens One Dark của prototype. Editor không phải chrome prototype nên literal light nằm ở app.css có comment giải thích).
- **3 Result Grid Execute/Cancel/Reset** ✅ (rename Apply→**Execute**, Discard→**Reset**; THÊM **Cancel** hiện khi đang chạy → `cancelQuery` (registry cancel T11). Preview diff + editable inline giữ nguyên. CH vẫn Generate mutation).
- **4 Cây relational hiển thị TẤT CẢ databases** ✅ (MySQL/MariaDB đã đúng từ trước — `SCHEMATA` liệt kê mọi DB làm schema node; PG = section "Databases" (AUDIT-2); THÊM MSSQL `databases()` = `sys.databases` WHERE `database_id>4 AND state=0` (loại system DB) + wire `LiveConnection::databases` arm + `open_database` cho phép PG|MSSQL + frontend load/section cho postgres|mssql. Integration `mssql_list_databases_excludes_system`).
- **5 Copy đa-định-dạng Result Grid** ✅ (pure `export/clipboard.ts`: `formatClipboard(fmt, {headers,rows,table,keyColumns})` — tsv/csv/json/sql-insert/sql-update/markdown; export `sqlLiteral` từ rows.ts để reuse. ResultGrid context menu: Copy cell/row/column + "Copy N row(s) as" → 6 format; selection = selectedRows|selectedCell|all. Unit `clipboard.test.ts` (12) + e2e `result-copy.spec.ts`).
- **6 Gap analysis** — GAP_REVIEW.md là snapshot CŨ (pre-T10); phần lớn "Missing" ở đó nay đã xong (T14 export wizard, T15 generate scripts, T18 explorer depth+Ctrl+F, T22 backup). Gap THỰC còn lại: Generate Test Data, Copy Table to… (cross-conn), Result Grid Group-By popover (Chart view có aggregation), proc/func Execute/Rename (Show Definition/Drop đã có T18), MSSQL Azure AD auth, Index/FK manager tab riêng, A5 streaming I/O (export/generate còn buffer RAM), Kafka ACL + NATS NKey-JWT (deferred cần broker/auth ngoài).
- Gates AUDIT-3: check 0/0, tokens:check (pre-existing 173 vi phạm ngoài phạm vi; 0 mới), vitest 200, playwright 36, Rust unit 72, integration PG/MSSQL databases + PG infinity EXIT=0.

## AUDIT-4 (yêu cầu người dùng: pagination + tree per-database + inline edit; rồi kế hoạch T27→T31)
- **Item 1 — Result Grid pagination** ✅ (pure `grid/paging.ts` `pageWindow(rowCount,pageSize,page)` unit-tested (6); ResultGrid: client-side pager (row range + page-size 100/200/500/1000 + ⏮◀▶⏭), chỉ cho query result (`!editable` — Table Viewer đã server-side paginate). Index tuyệt đối `ri = pageOffset + vi.index` giữ edits/selection đúng; inserted rows chỉ hiện trang cuối. e2e `result-pager.spec.ts`. Gates: check 0/0, vitest 206, playwright 37.
- **Item 3 — Inline edit Result Grid** ✅ ĐÃ CÓ từ AUDIT-3 (Execute/Cancel/Reset).
- **Item 2 — Explorer tree per-database** ✅ DONE (backend + frontend).
  - BACKEND: `attach_database(conn_id, db)` → sub-connection nội bộ `{conn_id}::{db}` (idempotent; current DB → trả `conn_id`); `disconnect` sweep `{id}::*`; `resolve_credentials` dùng chung; introspection dùng LẠI `list_*` với sub-id. Integration `pg_connection_to_other_database_sees_its_own_catalog` EXIT=0.
  - FRONTEND: ObjectExplorer nhánh relational — **KHÔNG rewrite lớn** (mẹo: giữ nguyên schema loop; chỉ đổi `base` depth). PG/MSSQL: current DB = header node "●…current" ở depth 0, schema loop nest dưới (base=1); các DB khác = node expandable ở cuối, `toggleForeignDb` → `ipc.attachDatabase` → subId → `explorer.loadSchemas(subId)` → subtree gọn (schema → Tables/Views/Procedures/Functions/Triggers/Sequences → names, dbl-click table → `openTableViewer(subId,…)`). Bỏ section "Databases" phẳng + `open_database`/adopt. **KHÔNG duplicate connection.** Specs vẫn xanh vì current DB tự-nest, `getByText('public'/'Tables'/'students')` vẫn tìm thấy. MySQL/MariaDB giữ nguyên (SCHEMATA đã đúng). e2e `explorer-databases.spec.ts`. Gates: check 0/0, vitest 206, playwright 38.
- **T27 — Grid Group-By popover** ✅ DONE. Pure `grid/groupby.ts` (`buildGroups` tree + subtotals/grand-total, `computeAgg` count/sum/avg/min/max, `buildGroupSql` cho server-side khi result bị truncate) unit-tested (9). ResultGrid: pager "Σ Group by" popover (chọn 1+ cột + agg) → grouped view collapsible (client-side trên toàn bộ result in-memory). e2e `group-by.spec.ts`. Gates: check 0/0, vitest 215, playwright 39.
- **⏸ STOP (a):** sau T27 phải trích 11 Partial từ GAP_REVIEW.md, xếp theo effort, đề xuất slot T-P1..n, CHỜ user chốt thứ tự trước khi tiếp T24.
- **T24 — Streaming I/O core** ✅ DONE (export). `PgDriver::stream_export` (sqlx `.fetch()` cursor → BufWriter, 1 row/lần, CSV/JSON/SQL) + `commands/export.rs` (`export_query_to_file` với `tauri::ipc::Channel<u64>` progress + `cancel_export` qua `ExportCancels` AtomicBool map) + `state.export_cancels`. Feature flag `settings.streamingIo` (default OFF → path Blob cũ là fallback); ExportDialog: khi flag on + Tauri + postgres + csv/json/sql → save-dialog + streaming + progress + Cancel. **Chỉ PG hiện tại** (khác → error "supports PostgreSQL"). Generate Scripts vẫn buffer (follow-up). Integration `pg_stream_export_million_rows_to_file` (1M rows, bounded) + `pg_stream_export_cancel_stops_early` EXIT=0. Gates: check 0/0, vitest 215, playwright 39, rust unit 72. STOP (a) đã hỏi user → chọn "Proceed to T24"; các T-P residual hoãn.
- **T25 — Copy Table to… (cross-connection)** ✅ DONE. Pure `copy/types.ts` (`classifyType`→family, `mapColumnType`/`mapColumns` per dest dialect, `buildCopyDdl` reuse `genCreate`) unit-tested (6). `CopyTableDialog.svelte` + `copyWizard` store + ObjectExplorer "Copy to…" ctx item: chọn dest connection (relational, connected) + schema + table → dry-run DDL preview (dialect đích) → Run = CREATE dịch DDL + copy data **paged** (buildExportSelect đọc + buildInsert ghi, PAGE=2000, bounded) + progress + Cancel (cancelFlag) + verify count(*) ở đích. Frontend-orchestrated (2 connections) — KHÔNG cần backend copy command. e2e `copy-table.spec.ts` + integration `sqlite_accepts_copy_mapped_types` (DDL mapper output chạy thật trên SQLite). Gates: check 0/0, vitest 221, playwright 40, rust unit 72. **Follow-up:** copy cross-engine data-type edge cases (bytea/json) + FK/index carry-over chưa làm.
- **T26 — Generate Test Data** ✅ DONE. Pure `testdata/generate.ts` — seeded mulberry32 RNG (deterministic) + per-column generators (sequence/number/decimal/bool/name/email/phone/date/timestamp/uuid/enum/text/fk/null); tôn trọng NOT NULL (không emit null), UNIQUE (fold row index), FK (giá trị lấy từ parent pool). Unit-tested (9: determinism, unique-no-dup, fk∈pool, notnull, enum, range). `GenerateTestDataDialog.svelte` + `testDataWizard` store + ObjectExplorer "Generate Test Data…" ctx item: per-column generator dropdown (+enum values) + row count + preview + Run (fetch FK parent pools → generate → batched INSERT 1000/batch). e2e `test-data.spec.ts` + integration `pg_test_data_contract_respects_fk_and_unique` (300 rows, FK+UNIQUE, no violation). Gates: check 0/0, vitest 230, playwright 41, rust unit 72. **Follow-up:** multi-table topological insert (hiện single-table, parent phải có sẵn rows).
- **T28 — Proc/Func Execute + Rename** ✅ DONE. Pure `sql/routines.ts`: `genRenameRoutine` (PG `ALTER {FUNCTION|PROCEDURE} …(argtypes) RENAME TO`, MSSQL `EXEC sp_rename`, MySQL/others → note) + `buildCall` (procedure→CALL/EXEC, function→SELECT, table_function→SELECT * FROM) — unit-tested (6). `ExecuteRoutineDialog.svelte` + `execRoutineWizard` store: nhập args theo signature (IN/INOUT, format literal theo type) → buildCall → mở SQL tab. Rename qua `window.prompt` → genRenameRoutine → SQL tab review. Context menu proc/func: Execute…/Rename… (T18 Show Definition/Drop giữ nguyên). **BẪY Svelte 5 gặp phải:** `{#if store.open}` (store là class $state singleton) đôi khi KHÔNG thiết lập subscription → dialog không mở dù open=true; fix = dùng `const dlgOpen = $derived(store.open)` rồi `{#if dlgOpen}` (áp cho GenerateTestDataDialog; Copy/Exec pass sẵn nhờ top-level derived đọc store field). e2e `proc-exec.spec.ts` + integration `pg_rename_function` + `mssql_rename_procedure` EXIT=0. Gates: check 0/0, vitest 236, playwright 42, rust unit 72.
- **TODO ngoài scope phát hiện khi làm T28:** MSSQL `CREATE PROCEDURE/FUNCTION/VIEW/TRIGGER` từ SQL editor SẼ LỖI vì driver route qua `execute()`=sp_executesql (CREATE PROC phải first-in-batch). Cần route CREATE-DDL qua `simple_query` (như T16 đã làm cho SET). Chưa fix (ngoài scope T28).
- **T29 — Index/FK Manager tab** ✅ DONE. Pure `sql/indexes.ts`: `genCreateIndex` (PG USING trước cols / MySQL USING sau / SQLite bare), `genDropIndex` (MySQL/MSSQL cần table; PG schema-qual; SQLite bare), `genAddForeignKey` (reuse genForeignKey), `genDropForeignKey` (MySQL DROP FOREIGN KEY vs others DROP CONSTRAINT) — unit-tested (8). Tab type `index-manager` + `tabs.openIndexManager` + `IndexManager.svelte` (list indexes+FKs, create/drop index form + add/drop FK form với live DDL preview, missing-index suggestions T17, refresh tree node qua `explorer.refresh`). Context menu table: "Manage Indexes & FKs…". Fixed demo bug (list_indexes/list_constraints fell through to routines payload). Áp `$derived` open-flag fix (T28 quirk) cho Execute/CopyTable dialogs. e2e `index-manager.spec.ts` + integration `pg_index_fk_crud` + `mysql_index_fk_crud` (create/drop index + add/drop FK, verify qua catalog) EXIT=0. Gates: check 0/0, vitest 244, playwright 43, rust unit 72.
- **T30 — ClickHouse MV/Dictionary create** ✅ DONE. Pure `sql/clickhouse_ddl.ts`: `buildCreateMaterializedView` (TO | ENGINE+POPULATE, validate name/select + TO/ENGINE conflict) + `buildCreateDictionary` (columns/PRIMARY KEY/SOURCE/LAYOUT/LIFETIME, validate required) — unit-tested (5). `chCreateWizard` store + `ClickHouseCreateDialog.svelte` (guided form 2 mode + live validated DDL preview) + schema ctx menu (CH only) "Create Materialized View… / Create Dictionary…". Run qua execStatement → refresh tree. e2e `ch-create.spec.ts` + integration `clickhouse_create_mv_and_dictionary` (tạo MV+Dictionary thật, verify system.tables/system.dictionaries) EXIT=0. Gates: check 0/0, vitest 249, playwright 44, rust unit 72.
- **T31 — MSSQL Azure AD auth** ✅ DONE (Service Principal). Pure `connections/aad.rs`: SP client-credentials OAuth2 flow qua **reqwest** (KHÔNG cần MSAL dep) — `sp_token_url`/`sp_token_form`/`parse_token_response`/`TokenCache.valid` (skew)/`redact` (không log secret)/`parse_sp_user` (clientId@tenant) — unit-tested (5). `acquire_sp_token` (live). mssql.rs connect auth=='aad-sp' → parse user → acquire token → `AuthMethod::aad_token` (tiberius 0.12 hỗ trợ). ConnectionForm: MSSQL auth option "Azure AD (Service Principal)" + hint. Integration `mssql_aad_service_principal` `#[ignore]` (cần Azure thật, đọc env, chạy tay). **BẪY Svelte 5 (T31 note):** dialog gate `{#if store.open}` VÀ `$derived(store.open)` đều có thể MISS cross-component tracking khi store là class $state singleton toggled từ component khác → fix chắc chắn = `let dlgOpen=$state(false); $effect(()=>dlgOpen=store.open)` (đã áp cho CH/Copy/TestData/ExecRoutine dialogs). Gates: check 0/0, vitest 249, playwright 44, rust unit 77.
## AUDIT-5 (post-T31) — 11 mục UI người dùng (relational-first, tham chiếu DataGrip):
- **Batch 1 ✅** (commit): (3b) bỏ nút Convert/Split (SqlWorkspace) + Saved/History (TitleBar). (6) "Import Data…" vào table context menu. (7) Explorer filter CHẠY: `searching` derived → auto-expand folders (`searching || expanded.has`) + auto `loadSchemaChildren` mọi schema khi filter → reveal matches. (2) Result Grid cột "No." (gutter số thứ tự, sticky-left) + keyboard nav (Arrow Up/Down/Left/Right + Tab/Shift+Tab di chuyển selectedCell). (9) multi-select: click cột No (shift/ctrl) — trước đây clickRowNumber không reachable vì cells stopPropagation. (8) ER drag&drop hardening: dragstart set effectAllowed='copy' + text/plain fallback; onDrop đọc cả 2 mime + không reject khi tables chưa load. e2e `audit5-ui.spec.ts` (filter + No column). Gates: check 0/0, vitest 249, playwright 44.
- **Batch 2 ✅** (commit): (1)(10) **DB dropdown trong Query Editor** — SqlWorkspace thêm chip "Database" cạnh connection dropdown (relational: PG/MSSQL qua `list_databases`, MySQL/MariaDB/CH qua `list_schemas` vì schema==database); chọn DB → `tab.state.database` (persist) + `resolveRunConn()` = `attachDatabase` sub-connection `{connId}::{db}` khi khác DB gốc (giữ connection dropdown = base profile); run/runAtCursor/cancel + ResultPanel connId dùng `runConnId` (grid edit đúng DB). (5) **Ctrl/Cmd+S save-to-file** — trong Tauri: `saveFileDialog` `.sql` + `ipc.writeTextFile` (đánh dấu tab sạch); browser fallback = snippet cũ. (4) **MySQL relational render — 2 BUG THẬT được phát hiện & sửa** (đây là lý do "MySQL chưa thể hiện được"):
  - (a) `information_schema` string columns MySQL 8 trả charset *binary* → sqlx type = VARBINARY → `row.get::<String>` **PANIC** → `list_schemas`/`list_tables`/`columns`/`triggers`/`routines`/`foreign_keys`/`constraints`/`indexes` chết → cây Explorer rỗng. Fix: helper `text()`/`text_opt()` (try String → fallback Vec<u8>→UTF-8 lossy) áp cho MỌI string decode trong introspection.
  - (b) `sqlx::query(sql).execute()` dùng PREPARED protocol → MySQL từ chối `CREATE TRIGGER` (& SHOW CREATE, vài DDL) với error 1295. Fix: `fetch_all`/`execute` helper truyền bare `&str` qua `Executor` → TEXT protocol (COM_QUERY); path tham số hoá (exec_params) vẫn prepare. (Cùng lớp bug với MSSQL SET/CREATE-DDL đã ghi.)
  - Integration `mysql_explorer_tree_introspection` (seed table+view+trigger → schemas/tables/columns/triggers verify) + `mysql_roundtrip` + `mysql_index_fk_crud` + `mariadb_roundtrip` EXIT=0. (3a) per-database object folders ĐÃ render sẵn (schema loop + foreign-db subtree Tables/Views/Procedures/Functions/Triggers/Sequences). e2e `audit5-ui.spec.ts` +DB dropdown test. Gates: check 0/0, vitest 249, playwright 45, Rust lib 77.
- **Batch 2 fixup ✅** (2 bug người dùng báo sau batch 2):
  - **Run báo "Tab has no connection" dù đã chọn connection+database** — root cause: DB dropdown chọn DB khác → `resolveRunConn` trả sub-connection id `{baseId}::{db}` (attach_database), nhưng id này KHÔNG có trong `connections.profiles` frontend → `results.run` `byId(connId)` undefined → toast. Fix: `results.run` tách `baseId` (phần trước `::`) để lookup profile + guard connected (sub-connection đã được attach_database connect sẵn), vẫn exec trên sub-id (backend registry có nó). ResultPanel/grid edit dùng runConnId=sub-id OK.
  - **Context menu không xuất hiện trên database name + table (PG/MSSQL)** — foreign-database subtree (các DB khác current) render KHÔNG có context menu; current DB header cũng không. Fix: parameterize helpers (`openData`/`newQuery`/`genSqlTab`/`copyDdl`/`stmtTab`/`genTableScript`/`columnsOf`/`showDefinition`) nhận optional `cid` + optional `database` (bind SQL tab tới foreign DB qua `tab.state.database`); thêm `dbMenu` (foreign db node: New Query in <db>/Expand/Copy/Refresh), `curDbMenu` (current db header: New Query/Generate Scripts/Backup/Compare/Copy/Refresh), và `fObjMenu` (foreign-db object rows: Open Data/New Query/Import/Export/Copy to/Test Data/Design/Indexes/Generate SQL+Scripts+DDL/Show Definition/Copy/Refresh/Truncate/Drop — tất cả chạy trên sub-connection `sub`). e2e: DB dropdown test mở rộng Run-against-picked-db + `foreign database node has a context menu`. Gates: check 0/0, vitest 249, playwright 46.
- **Batch 3 ✅** (3 yêu cầu người dùng — context menu đầy đủ + Create per-type + Design Table hiện conn/db):
  - **byId resolve sub-connection** (ROOT FIX): `connections.byId('{base}::{db}')` giờ trả base profile (trước → null → mọi tab mở trên foreign DB — TableDesigner/IndexManager/ErDiagram — bị `systemType='orphan'` → hỏng DDL). Thêm `connections.databaseOf(id)` = db sau `::` hoặc base.database.
  - **Create <type>… trên folder** (current + foreign subtree): pure `sql/create-templates.ts` (`createTemplate(system, view|procedure|function|trigger|sequence, schema)` — skeleton per-dialect: PG `$$` body + trigger emit cả function; MySQL/MSSQL BEGIN…END; PG-only sequence; CH/SQLite unsupported→comment) unit-tested (9). Folder menu: Tables→New Table…/Import; Views→Create View…; Stored Procedures→Create Procedure…; Functions(+MSSQL TVF/Scalar)→Create Function…; Triggers→Create Trigger…; Sequences(PG)→Create Sequence… (mở SQL tab bound tới đúng DB).
  - **Foreign-db schema node menu** = View ER Diagram/New ER Diagram/Scan Indexes/New Table/Create View·Proc·Func·Trigger/Generate Scripts/Refresh (chạy trên sub-connection).
  - **Design Table header hiện connection + database** (`profile.name` + `databaseOf` + schema); Save vẫn execStatement trên `tab.connectionId` (sub-id → đúng foreign DB) + refresh subtree.
  - Explorer helpers parameterize thêm `database?` (bind foreign-db SQL tab qua `tab.state.database`). e2e: `object folders offer Create <type>` + `table designer header shows connection and database`. Gates: check 0/0, vitest 258, playwright 48, tokens 0 mới.
- **Batch 4 ✅** (người dùng: "bỏ mục filter"): gỡ ô "Filter tree (Ctrl+F)" khỏi ObjectExplorer + Ctrl+F handler + auto-expand-khi-search effect + `searchEl`/`search` state (thay `matchSearch`→always true, `searching`→`false` để giữ nguyên hàng chục guard `{#if searching || expanded…}` = expand-on-click). Bỏ import `ui` thừa. Cập nhật test: xoá `audit5-ui` filter test + gỡ 2 assertion filter khỏi `explorer-depth`. Gates: check 0/0, playwright 47.
- **Batch 5 ✅** (người dùng: "Result Grid tham khảo font DataGrip"): bundle **JetBrains Mono** (font của DataGrip) qua `@fontsource/jetbrains-mono` (self-host, import weights 400/500/600/700 trong `main.ts`) → mọi `.mono` (Result Grid + editor + tree meta) render đúng font thay vì fallback Consolas. Result Grid table thêm `font-variant-numeric:tabular-nums` + `font-feature-settings:'tnum','zero'` (id/timestamp thẳng cột). **Prototype 1:1 giữ nguyên**: prototype CSS vốn khai báo `--font-mono:'JetBrains Mono'` nhưng baseline không nạp được → thêm `injectMonoFont` (base64 woff2, no network/CORS) vào CẢ `openPrototype` + `openApp` trong helpers → pixel-diff so JetBrains-vs-JetBrains (sidebar-connections region 3.12%→0.00%). Gates: check 0/0, playwright 47, tokens 0 mới.
- **AUDIT-6 (6 mục, DataGrip-inspired)** ✅:
  - **1/2 New Query** — thêm vào schema/database context menu (schemaMenu + foreign fSchemaMenu); tables/views đã có.
  - **3 Result Grid multi-select màu xanh** — row selected dùng `color-mix(--primary 30%)` + gutter 65% (số trắng) thay vì tint 16% mờ.
  - **4 Save-before-close ghi ra file** — nút Save trong SaveBeforeCloseDialog mở save-dialog `.sql` mỗi tab dirty (Tauri) rồi đóng; browser fallback = mark-clean. Mọi đường đóng tab đã route qua `requestClose` → luôn hiện prompt cho tab dirty.
  - **5 Alter/Drop/Execute per type** — "Alter…" (mở DDL thật qua objectDefinition, dialect-correct) trên view/proc/func/trigger + foreign; Sequence context menu (Alter SEQUENCE/Drop SEQUENCE/Copy); Execute (proc/func) + Drop đã có.
  - **6 Compare databases cùng 1 connection** — SchemaCompare thêm dropdown database cho src/tgt (PG/MSSQL list_databases, MySQL/MariaDB/CH list_schemas), resolve qua attach_database sub-connection; migration mở trên target DB; icon Add(＋)/Changed(≠)/Delete(－) đã có (thêm icon vào DDL-diff panel badge). Guard "same database".
  - Gates: check 0/0, vitest 262, playwright 54, tokens 0 mới.
- **CÒN LẠI:** DataGrip icon set đầy đủ (đã làm folder icon cho database node) — còn lại thẩm mỹ nhỏ, ưu tiên thấp.
- **AUDIT-7 — Table Designer đầy đủ tabs (DataGrip)** ✅: pure `sql/table-designer.ts` (`buildTableDdl(system, model, isNew)` + `buildTrigger` + `columnDef`) sinh DDL đúng từng dialect — new table = 1 CREATE (cột + table-level PK + inline UNIQUE/CHECK/FK) + CREATE INDEX/TRIGGER; existing = ALTER ADD cho object MỚI (item có `existing` = seeded, không tái tạo). Quirks: MSSQL `ADD` (không COLUMN) + trigger AFTER/INSTEAD OF (BEFORE→AFTER + warn); SQLite không ALTER-ADD CHECK/UNIQUE/FK (UNIQUE→CREATE UNIQUE INDEX, CHECK/FK→warning); ClickHouse ENGINE/ORDER BY, bỏ constraint/trigger + warn. `TableDesigner.svelte` thêm tab bar **Fields · Indexes · Foreign Keys · Uniques · Checks · Triggers** + seed object thật của bảng (existing, read-only) + **Ctrl/Cmd+S** (guard active tab) chạy từng statement, dừng + refresh khi lỗi. Unit `table-designer.test.ts` (17, đủ 6 dialect × new/alter + triggers) + visual (tabs + CREATE INDEX trong preview + Ctrl+S). **Integration end-to-end** (`{pg,mysql,mssql,sqlite}_table_designer_create_alter_end_to_end`): chạy CHÍNH XÁC DDL mà `buildTableDdl` sinh (new table = CREATE + inline UNIQUE/CHECK/FK + CREATE INDEX + CREATE TRIGGER; rồi ALTER ADD) trên container thật, verify qua catalog — PG/MySQL/MSSQL/SQLite đều EXIT=0 (MSSQL bỏ CREATE TRIGGER vì cần batch riêng; SQLite UNIQUE→CREATE UNIQUE INDEX). Gates: check 0/0, vitest 289, playwright table-designer 2 + audit5-ui 6, tokens 0 mới (gỡ 2 vi phạm cũ trong TableDesigner).
- **AUDIT-8 — Streaming trong Explorer (Kafka topics / NATS streams→subjects)** ✅: pure `stream/explorer.ts` (`kafkaTopicRows`/`natsStreamRows`/`topicMessageCount` — lọc internal, dedup subjects, meta "N part·M msg") unit-test (4). **Kafka**: connect qua host:port (bootstrap ghép sẵn ở `kafka_params`); ObjectExplorer nhánh `isKafka` liệt kê từng topic (single-click→KafkaConsumer messages; ctx: View/Produce/**Clear messages**/**Delete topic**). Backend mới `KafkaDriver::purge_topic` (rdkafka 0.38 `delete_records` tới high watermark — giữ topic) + `kafka_purge_topic`. **NATS**: nhánh `isNats` = stream (expandable) → subjects; click subject→tab `nats-subject` (`NatsSubjectMessages.svelte` gọi `js_subject_messages`); ctx subject: View/**Clear messages**/**Delete subject**. Backend mới `js_subject_messages` (ephemeral pull consumer filter+fetch), `js_purge_subject` (purge filter), `js_remove_subject` (update stream config, chặn xoá subject cuối) + 3 command. Explorer store `streaming` cache + `loadStreaming`/`refreshStreaming`; row snippet thêm `openOnSingleClick` (leaf mở bằng 1 click, giữ select-only cho node expandable). Integration container thật: `kafka_purge_topic_clears_messages` (produce 5→purge→low==high→topic còn) + `nats_subject_messages_purge_and_remove` (browse 3, purge 1 subject giữ subject kia, remove subject khỏi config, chặn subject cuối) — cả 2 EXIT=0. Visual `streaming-explorer.spec.ts` (2). Gates: check 0/0, vitest 293, playwright +2.
- **AUDIT-9 — Alter/Execute view·proc·func·trigger chạy đúng mọi DB relational** ✅: 3 root cause đã fix. (1) **Alter sinh DDL KHÔNG chạy lại được** — trước chỉ mở raw definition (PG `pg_get_viewdef` = bare SELECT; các engine khác `CREATE …` → "already exists"). Pure `sql/alter.ts::toAlterStatement(system,kind,schema,name,def)` chuyển thành statement re-runnable: PG view→`CREATE OR REPLACE VIEW … AS <body>`, func/proc giữ CREATE OR REPLACE, trigger→`CREATE OR REPLACE TRIGGER` (PG14+); MySQL view→`CREATE OR REPLACE`, routine/trigger→`DROP IF EXISTS`+CREATE; MSSQL→`CREATE OR ALTER`; SQLite view/trigger→`DROP IF EXISTS`+CREATE. `alterObject` fetch def→transform→mở SQL tab. Unit `alter.test.ts` (8). (2) **Splitter cắt body routine** — `sql/statements.ts` chưa hiểu dollar-quote PG (`$$…$$`/`$tag$`), `;` trong body function bị cắt → CREATE/ALTER FUNCTION lỗi khắp PG. Thêm mode `dollar` (+ beginDepth sẵn có). Unit +2. (3) **MSSQL driver** — CREATE/ALTER/DROP DDL route qua `simple_query` (raw batch, `is_raw_batch`) để `CREATE OR ALTER PROC/FUNC/VIEW/TRIGGER` là first-in-batch; `EXEC/EXECUTE` route qua rows-path (`is_exec`) để result set proc hiện ra grid. Integration container thật: `{pg,mysql,mssql}_alter_and_execute_objects_end_to_end` (create→execute→alter→verify effect) + `sqlite_alter_view_and_trigger_end_to_end` — tất cả EXIT=0; mssql_table_designer regression EXIT=0. Gates: check 0/0, vitest 303.
- **AUDIT-9b — Execute popup thực thi ngay** ✅: `ExecuteRoutineDialog` đổi nút **"Open in SQL tab" → "Execute"**; `openSqlTab` thêm `autoRun` (state.autoRun) → SqlWorkspace `$effect` chạy 1 lần khi editor mount (guard `autoRan`, clear cờ) → kết quả proc/func hiện thẳng result grid. Unit `tabs.test.ts` (+1 autoRun) ; visual `proc-exec.spec.ts` (nút Execute + auto-run → "Single Row" xuất hiện). Execute thật ra kết quả đã integration-covered bởi `{pg,mysql,mssql}_alter_and_execute_objects_end_to_end` (SELECT func / CALL / EXEC trả value). Gates: check 0/0, vitest 304.
- **AUDIT-10 — Folder Filter kiểu SSMS (context menu)** ✅: trước đây ô filter luôn hiện dưới mỗi folder (AUDIT-5 batch4 gỡ top-filter nhưng per-folder vẫn always-on) → đổi thành: ẩn cho tới khi chọn **"Filter…"** trong context menu của folder (Tables/Views/Stored Procedures/Functions/TVF/Scalar/Triggers/Sequences + foreign-db subtree). Chọn Filter… → expand folder + hiện ô nhập (auto-focus) + lọc case-insensitive substring; **"Remove Filter"** (menu) hoặc nút × / Esc để xoá. Pure `explorer/filter.ts` (`objectFilterMatch`/`filterByName`) unit-test (3); component dùng `objectFilterMatch`; snippet `filterMenuItems(key)` chèn vào mọi folder menu (tách `createFnFolderMenu`→`tvfFolderMenu`/`scalarFolderMenu` để mỗi folder có key riêng). e2e `folder-filter.spec.ts` + cập nhật `audit-hotkeys` per-folder filter mở qua context menu. Không có DB round-trip (lọc client-side) nên "integration" = e2e app-level.
  - **Clear Filter (bắt buộc)**: ô filter có nút **×** (xóa text, giữ box để lọc lại) + nút **"Clear"** (xóa hẳn + đóng box) + context menu **"Clear Filter"** + **Esc**. `clearFolderFilter` LUÔN xóa text (folderMatch key theo text nên không để lại filter ẩn khi đóng box). Demo thêm view/trigger/procedure thứ 2 để test lọc có ý nghĩa. e2e clear cho đủ 4 folder **Views / Stored Procedures / Functions / Triggers** (lọc ẩn 1 → Clear Filter → khôi phục cả 2) + unit `filter.test.ts` (clear→"" khôi phục full list). Gates: check 0/0, vitest 308, playwright folder-filter (5) + regressions xanh.
- **✅ TOÀN BỘ T24→T31 + T27 HOÀN TẤT** (merged main). Còn lại chỉ follow-up nhỏ + T-D deferred (Kafka ACL / NATS NKey-JWT — cần setup broker/JWT ngoài). Follow-up: streaming cho engine khác PG + Generate Scripts streaming + Row Count & Stats + Trigger Enable/Disable + MSSQL CREATE-DDL via simple_query + AAD Interactive/device-code.
- **TODO ngoài scope (ghi lại, chưa làm):** —
- **Kế hoạch T27→T31** (xem prompt người dùng): T27 Group-By popover → STOP (a) chốt thứ tự 11 Partial; T24 streaming I/O; T25 Copy Table to…; T26 Generate Test Data; T28 proc/func Execute+Rename; T29 Index/FK manager; T30 CH MV/Dictionary; T31 MSSQL Azure AD. T-D Kafka ACL + NATS NKey/JWT = config surface stub, giữ Deferred.

## AUDIT-11 (8 mục người dùng — relational depth; user chọn "per-tab connection" + "backend blockers first"):
- **8 Data types đầy đủ mọi relational** ✅: pure `sql/datatypes.ts` (`dataTypes(system)` — PG/MySQL/MariaDB/MSSQL/SQLite/ClickHouse, mỗi engine 20–60 kiểu, MariaDB = MySQL + uuid/inet4/inet6). `designerTypes` (ddl.ts) giờ delegate về `dataTypes` (single source). Unit `datatypes.test.ts` (6). Feed dropdown Table Designer (item 3).
- **7 MySQL Stored/Function execute — DataType** ✅: **Root cause KHÔNG phải driver** (integration `mysql_execute_stored_routines_datatypes` chứng minh driver chạy đúng SELECT func()/CALL proc() cho INT/DECIMAL/DATETIME/VARCHAR + result set). Bug thật ở **format tham số**: `ExecuteRoutineDialog.literal()` dùng `\bint\b` → **miss tinyint/mediumint/int(11)/unsigned/year** → quote sai thành string. Fix: pure `literalArg(dataType, raw)` trong `sql/routines.ts` (numeric regex anchored `^(...)(?![a-z])`, không nhầm interval/point) + unit (5). Splitter `statements.ts` đã xử lý BEGIN…END + DECLARE typed (test sẵn) nên CREATE routine OK.
- **6 Query Editor tab độc lập — per-tab dedicated connection ✅**: (a) đóng tab huỷ query — `results.TabExecution.connId` + `results.cancelAndClear` + `tabs.forceClose`. (b) FULL isolation — backend `open_tab_connection(connId,tabId,db)` tạo connection riêng `{base}#tab-{tabId}` (idempotent, reconnect khi đổi DB) + `close_tab_connection` + `disconnect` sweep thêm prefix `{id}#`; đăng ký lib.rs. Frontend: pure `baseConnId()` (strip `#tab-` rồi `::`) dùng trong `connections.byId`/`databaseOf` + `results.run`; `SqlWorkspace.resolveRunConn` mở per-tab connection; `forceClose` gọi `closeTabConnection` cho sql-editor tab. Introspection/Explorer vẫn dùng base connection → hung query 1 tab KHÔNG block tab khác/Explorer. Integration `tab_connections_are_isolated_hung_query_does_not_block` (pg_sleep(30) tab-1 + SELECT 1 tab-2 xong trong 8s + cancel) EXIT=0. e2e query-run (result-pager/group-by) xanh với path per-tab (demo trả `{cid}#tab-{id}`).
- **1,2 Compare Execute ở cả Diff + Script + dropdown ✅**: SchemaCompare (chung cho "Compare database" & "Compare schemas") — thêm nút **Execute** + **Open script** vào Diff view (trước chỉ ở Sync Script); `execMigration` chạy từng statement migration xuống TARGET (attach sub-connection theo db đích) + re-diff. Dropdown connection: `options` giờ = **mọi relational profile** (connected hoặc chưa) thay vì chỉ connected → chọn được; `ensureConnected` mở connection on-demand trong compare(). **BẪY test**: title "…target database" của nút Execute đụng `getByTitle('Target database')` (strict-mode) → đổi title "Run this migration on the target now". e2e schema-compare 3/3.
- **5 Open Connection states ✅**: `connections.connectErrors` (persist lỗi connect, clear khi connecting/success/disconnect). ObjectExplorer: branch `connecting.has(id)` → "Connecting to …" (◴); `!connected` + connectErrors → "Could not connect … <lỗi>" + nút **Retry** (thay "Connect"). ConnectionList dot: đỏ (error) khi connectErrors && !connected. e2e `connection-open.spec.ts` (Not connected → Connect → tree hiện). Connecting/failure transient khó e2e ở demo (demo connect luôn success tức thì) → code-covered.
- **4 Columns hiện cho MỌI relational ✅**: **Root cause** — `explorer.loadTableDetail` dùng `Promise.all([listColumns,listIndexes,listConstraints])`; nếu 1 trong 3 reject (index/constraint introspection lỗi trên 1 engine) → `Promise.all` reject → **vứt luôn columns đã load OK** → table expand ra rỗng. MySQL tình cờ cả 3 OK nên hiện columns. Fix: `Promise.allSettled` → columns luôn giữ dù index/constraint fail (degrade []). Unit `explorer.test.ts` (2: columns sống khi index/constraint reject; đủ 3 khi OK).
- **3 Table Designer ✅** (phần lớn đạt qua item 8): DataType dropdown = **full per-engine catalog** (item 8, `<datalist>` searchable) + caret ▾ (DataGrip-like) + `spellcheck=false`; `defaultColumnType(system)` (integer/int/INTEGER/Int64 — thay vì `types[0]` = 'smallint' sau item 8) cho blank col + id-seed PK. Grid Fields·Indexes·FK·Uniques·Checks·Triggers + tab giữa ô = native input/select order (sẵn). Unit `datatypes.test.ts` (+defaultColumnType) + e2e table-designer 3/3. tokens:check 0 vi phạm mới.
- **⚠️ AUDIT-11 REVISED — user test REAL app báo 1,2,3,4,6,7,8 vẫn Failed (chỉ 5 Pass).** Bài học: **Playwright chạy trên DEMO data (giả)** → "pass" KHÔNG validate hành vi thật. Integration test (container thật) mới đáng tin cho backend. Vòng sửa mới:
  - **1,2 Compare — thêm Indexes + snapshot robust**: `diff.ts` thêm `CmpIndex` + so sánh index theo (table,name) column-set/unique + migration CREATE/DROP INDEX (reuse `sql/indexes.ts`); `ObjectKind` += 'index'. SchemaCompare.snapshot: fetch indexes (non-primary) + **`.catch` mọi list call** (trước `listColumns` không catch → 1 bảng lỗi là ABORT cả snapshot → không hiện gì). Unit `diff.test.ts` +index (15). **Integration `pg_full_introspection_surfaces_every_object_type`** chứng minh driver PG trả VIEW/INDEX/FUNCTION/PROCEDURE/TRIGGER/SEQUENCE/columns → compare có data để hiển thị. (Views/Procs/Funcs/Triggers ĐÃ trong logic cũ; gốc lỗi = snapshot abort + thiếu indexes.)
  - **3,8 Table Designer type dropdown**: **root cause nghi ngờ** = `<datalist>` render KHÔNG đáng tin trong Tauri WebView2 → type dropdown rỗng dù demo (browser) hiện. Thay bằng component `TypeSelect.svelte` (custom searchable combobox, position:fixed thoát clip của grid scroll, full per-engine catalog + filter + free-text + keyboard). e2e table-designer mở dropdown → filter 'jsonb' → chọn → vào DDL. tokens 0 mới.
  - **4 columns**: đã `Promise.allSettled` trong `loadTableDetail`; integration chứng minh PG columns() OK. Nếu real app vẫn rỗng → cần diagnostics engine cụ thể + console error.
  - **CÒN NGHI (cần user xác nhận)**: các frontend fix chỉ chạy nếu **build lại** (`npm run tauri dev`). Item 5 "Pass" có thể do dot connecting CŨ (pre-existing) → chưa chắc frontend đã rebuild. Items 6 (per-tab conn) + 7 (MySQL routine literalArg) backend integration EXIT=0 nhưng cần rebuild backend + user báo lỗi thật nếu vẫn fail.
  - Gates: check 0/0, vitest 344, tokens 0 mới, e2e (schema-compare 3, table-designer 3, connection-open 1) xanh; integration `pg_full_introspection…` + `mysql_execute_stored_routines_datatypes` + `tab_connections_are_isolated…` EXIT=0.
- **7 MySQL routine execute — ROOT CAUSE THẬT tìm được (user xác nhận "trong MySQL", chạy `tauri dev`)**: procedure có param **OUT/INOUT** → dialog cũ truyền **literal** vào chỗ cần biến → MySQL báo lỗi "OUT or INOUT argument is not a variable" / sai số tham số. **Reproduce bằng integration** (`CALL p_calc(3,4.50,0,0.1)` literal → `.is_err()` PASS; session-var `SET @_total=NULL; SET @_tax=0.10; CALL p_calc(3,4.50,@_total,@_tax); SELECT @_total,@_tax` → total=13.50, tax=1.35). **Fix**: pure `buildRoutineExec(system,schema,kind,name,params,values)` — MySQL/MariaDB dùng session vars (`SET @_x` + CALL + `SELECT @_x AS x`), MSSQL `DECLARE @_x … EXEC … @_x OUTPUT … SELECT`, function/PG giữ SELECT/CALL IN-only. ExecuteRoutineDialog dùng nó (truyền TẤT CẢ params kèm mode). Guard `mode ?? ''` (demo mode undefined → crash). Unit `buildRoutineExec` (5) + integration mở rộng + e2e proc-exec xanh. **buildCall giữ lại** (backward compat/test).
- Gates cuối: check 0/0, vitest 349, integration mysql routine (OUT/INOUT) + pg_full_introspection + tab-isolation EXIT=0.
- **4 Columns KHÔNG hiện ở PG/MSSQL — ROOT CAUSE THẬT (user gửi screenshot: MySQL hiện, PG/MSSQL không)**: MySQL/MariaDB `schemaIsDatabase` → MỌI database render qua **main schema loop** (table rows `expandable:true` + render `detail.columns`) → columns hiện. PG/MSSQL `pgMssqlMultiDb` → chỉ **current database** ở main loop; các database KHÁC ở **foreign-db subtree** (ObjectExplorer ~1670) mà table rows **KHÔNG `expandable`, KHÔNG render columns**. App tables của user nằm ở database khác (non-default) → foreign subtree → không có columns. **Fix**: foreign-db table/view rows giờ `expandable:true` + `onClick` load `loadTableDetail(sub, schema, name)` + render `fsc.tableDetails[nm].columns` (mirror main loop, depth 4). e2e `explorer-databases` mở rộng: expand foreign db → schema → Tables → table → columns (`first_name`) hiện. ClickHouse/Cassandra: main loop / cassandra_tree (nếu vẫn lỗi cần confirm — nghi user over-list; MariaDB=MySQL path nên OK).
- Gates: check 0/0, vitest 349, e2e explorer-depth/databases/folder-filter (7) + table-designer + proc-exec + schema-compare xanh.
- **4b foreign-db column context menu**: columns thêm ở foreign-db subtree ban đầu thiếu context menu (main loop có). Thêm `fColMenu` (Copy Name / Copy as table.column / Set as Filter — dùng sub-connection). e2e mở rộng: right-click foreign column → menu hiện. check 0/0.
- **1c/2c Compare group + checkbox** (bước 1, sau bị chỉnh): group theo type + checkbox mọi object + `objectKey(d)=kind:name:idxTable` cho selection/genMigration.
- **1d/2d Compare — nest Index/Trigger DƯỚI Tables + searchable dropdowns** (user: "Index/Storeds/Views/Functions/Triggers phải là CHILDREN của Tables; dropdown connection+database cho phép search"):
  - **Nest**: Indexes + Triggers giờ là **con của table sở hữu** (không còn group riêng). `CmpRoutine.table` (trigger's table) + `ObjectDiff.idxTable` set cho cả index & trigger (owning table). SchemaCompare: mỗi table row expand → columns + nested index rows + nested trigger rows (snippet `nestedObj`, checkbox + status + trigger click→DDL diff). Table hiện dù identical nếu có index/trigger đổi (`tableVisible`). Views/Stored Procedures/Functions/Sequences = group riêng (KHÔNG thuộc table nào — đã giải thích). Group headers còn: Tables/Views/Stored Procedures/Functions/Sequences.
  - **Searchable dropdowns**: component mới `SearchSelect.svelte` (combobox fixed-list, filter theo label, position:fixed thoát clip). Thay 4 `<select>` (Source/Target connection + database) → `connOptions`/`srcDbOptions`/`tgtDbOptions`. e2e cập nhật: helper `pick()` (click→option) + test search (gõ 'mysql' → chỉ MySQL option).
  - Gates: check 0/0, vitest 349, tokens 0 mới, e2e schema-compare 3/3 (search + nested + 2-db).
- **1e/2e Compare — cây folder theo Tables** (user vẽ cây: Tables > TableName > Columns/Indexes/Triggers folders > items): mỗi table expand → **folder rows** `Columns`/`Indexes`/`Triggers` (snippet `folderRow`, indent px-38, expand riêng qua `expandedRows` key `${dkey}:columns|indexes|triggers`) → items (columns/nestedObj indent px-54). **Chỉ hiện folder có item** ("không lấy default để hiển thị" = bỏ folder rỗng). Views/Stored Procedures/Functions/Sequences vẫn top-level group (DB không gắn chúng vào 1 table cụ thể — đã note hỏi user). e2e cập nhật: expand table → click 'Columns' folder → 'first_name' hiện. Gates: check 0/0, vitest 349, tokens 170 (0 mới), e2e 3/3.
- **1f/2f Compare — auto-expand + checkbox mọi cấp** (user: "auto expand, columns cũng phải có checkbox, đầu mục Indexs/Storeds/Views/Functions/Triggers có checkbox check nhanh"): (a) auto-expand — `expandedRows`(collapsed) → `collapsedRows`(default EXPANDED) + `isRowOpen`; tables+folders mở sẵn. (b) column checkbox — `columnKey(table,col)` export; mỗi column row checkbox (changed = actionable, identical disabled); `genMigration` skip column không chọn; auto-select thêm changed-column keys. (c) folder select-all — `folderRow(fkey,label,keys[])` + checkbox `keysAllSelected`/`toggleKeys`; top-level groups đã có select-all. e2e: auto-expand → Columns/first_name + "Select all in Columns" checkbox visible. Gates: check 0/0, vitest 349, diff 15, tokens 0 mới, e2e 3/3.
- **1g/2g Compare — màu per-type + canh lề tree**: `KIND_COLOR`/`KIND_GLYPH` (reuse màu Explorer: table 5b9bd5, view b48ead, procedure e8923a, function e8c547, trigger e06c75, sequence 56b6c2, index 7f8a9e, column 9aa4b8) áp glyph+màu cho group header + object row + folder header + column/index/trigger rows. Indent đều theo cấp: group 14 → table 26 → folder 40 → item 56 (px); folder header thêm box-shadow màu type. tokens 0 mới (var(--hex-*) sẵn có). Gates: check 0/0, vitest 349, e2e 3/3.
- **1h/2h Compare — group header nổi bật hơn** (user: chưa in đậm/đổi màu rõ, chưa thấy rõ là group): brighten màu muted (index→cyan 56b6c2, column→green 98c379, sequence→amber d19a66) để phân biệt; group/folder header: **font-weight 800 + UPPERCASE + màu type**, background riêng (top group `var(--hover)`, folder `var(--header)` — khác nền item), **left accent bar** `box-shadow:inset px-3 màu` + border top/bottom → đọc rõ là header. Chevron+glyph+count cũng tô màu type. tokens 0 mới. Gates: check 0/0, vitest 349, e2e 3/3.
- **1i/2i Compare — Refresh + script syntax color**: nút **⟳ Refresh** (gọi `compare()` re-read 2 schema) ở CẢ Diff action bar VÀ Script action bar. Script migration `<pre>` giờ **syntax-color** qua pure `format/sql.ts` (`highlightSql`/`sqlTokenColor` — keyword/string/comment/number dùng `--syntax-*`, merge token cùng loại), render `{#each highlightSql(migration)}<span màu>`. Unit `sql.test.ts` (5, lossless + phân loại + merge). e2e: Refresh visible ở Diff & Script. Gates: check 0/0, vitest 354, tokens 0 mới, e2e 3/3.
- Gates sau AUDIT-11 (phần đã xong 6,7,8): check 0/0, vitest 338, integration `mysql_execute_stored_routines_datatypes` + `mysql_explorer_tree_introspection` + `tab_connections_are_isolated_hung_query_does_not_block` EXIT=0.
- **AUDIT-12 — Table Designer edit đầy đủ + dropdown search** (user: existing columns edit hoàn toàn; Columns/Ref.table/Ref.column dropdown search field sẵn có; thêm On Update + On Delete/On Update dropdown chuẩn SQL; Method dropdown chuẩn SQL):
  - **Edit existing**: bỏ `disabled={existing}` (Indexes/FK/Uniques). DDL drop+recreate khi sửa existing (`indexChanged`/`uniqueChanged`/`fkChanged` vs `orig` seed → DROP+CREATE/ADD); **rename column** (`columnRenamed`/`renameColumn` — PG/MySQL/SQLite RENAME COLUMN, MSSQL sp_rename) emit trước alterColumn. Seed `orig` cho columns(+name)/indexes/uniques/fks.
  - **Dropdown search**: `MultiSelect.svelte` (chips, searchable, position:fixed) cho Columns/Ref.columns (options = `colNames` bảng / `refColsCache` load theo Ref.table). `SearchSelect` (+prop `onChange`) cho Ref.table (`schemaTables` qua listTables), Method (per-dialect chuẩn SQL), On Delete + **On Update mới** (CASCADE/SET NULL/RESTRICT/NO ACTION/SET DEFAULT).
  - Unit `table-designer.test.ts` (+6). e2e: thêm cột email → pick trong index MultiSelect → CREATE INDEX. Gates: check 0/0, vitest 360, tokens 0 mới, e2e table-designer 3/3.
- **AUDIT-12b — dropdown UX + script color** (user: không chọn bằng phím được; type dropdown không scroll/không focus giá trị hiện tại/click lần 2 không mở; Script đổi tông màu):
  - **Fix cả 3 combobox** (TypeSelect/SearchSelect/MultiSelect): **click-twice bug** — thêm `onclick={openMenu}` (input giữ focus sau chọn → onfocus không bắn lần 2) + guard `if (open) return`. **Keyboard**: ArrowDown/Up khi đóng thì mở (không nhảy qua item hiện tại), Enter chọn `filtered[active]`. **Scroll-into-view**: `$effect` scroll `menuEl.children[active]` khi active đổi (bind `menuEl`). **Focus giá trị hiện tại**: openMenu set `active = options.findIndex(current)` (TypeSelect/SearchSelect).
  - **Script màu**: TableDesigner Scripts `<pre>` giờ syntax-color qua `highlightSql`/`sqlTokenColor` (như SchemaCompare).
  - e2e table-designer: keyboard Enter chọn 'bigint' + reopen (click-twice) + pick jsonb. Gates: check 0/0, vitest 360, tokens 0 mới, e2e table-designer 3/3 + schema-compare 3/3.
  - **AUDIT-12b fixup — Enter chọn khi search ra 1 giá trị** (user: search ra 1 giá trị (vd `end_date`) → không dùng phím lên/xuống để chọn được): root cause = `active` có thể lệch khỏi `filtered` (stale index sau khi filter thu hẹp) → Enter/highlight trỏ vào item không tồn tại. Fix cả 3 combobox: (1) Enter chọn `filtered[active] ?? filtered[0]` (luôn chọn được khi có ≥1 match, kể cả active lệch); (2) `$effect` clamp `active` về `[0, filtered.length-1]` mỗi khi filtered đổi. e2e index-column dùng keyboard (fill 'email' → Enter → chip thêm). Gates: check 0/0, e2e table-designer 3/3 + schema-compare 3/3.
  - **AUDIT-12c — type catalog đầy đủ + scroll chuột** (user: TableDesigner thiếu nhiều datatype + không scroll được bằng chuột):
    - **Scroll chuột bug (cả 3 combobox)**: listener `window.addEventListener('scroll', close, true)` (capture) đóng menu khi CUỘN BÊN TRONG list (event bubble tới capture) → không scroll được. Fix: `onScroll` bỏ qua khi `menuEl.contains(ev.target)` (chỉ đóng khi cuộn NGOÀI menu để menu fixed không trôi lệch).
    - **Datatypes đầy đủ** (`sql/datatypes.ts`): PG +int2/int4/int8/serial aliases, character varying/character, jsonpath, hstore, multiranges (PG14+), pg_lsn/txid_snapshot; MySQL +integer/dec/fixed/double precision/real + unsigned variants; MSSQL +json/timestamp/cursor/table; SQLite +INT2/INT8/UNSIGNED BIG INT/DOUBLE PRECISION/VARYING CHARACTER/NATIVE CHARACTER; ClickHouse +BFloat16/Decimal256/JSON/Nested/Nothing/Aggregate(Simple)Function/geo(Point/Ring/Polygon/MultiPolygon). Unit datatypes.test 6/6 (no dup, contains signature types). Gates: check 0/0, vitest 360, tokens 170 (0 mới), e2e table-designer 3/3.

## AUDIT-13 — Redis vào ObjectExplorer + key viewer nâng cấp (4 mục user):
- **NATS Time cột = UTC (Z)**: `NatsSubjectMessages.fmtTime` parse ISO (có offset server) → `new Date` → getter `getUTC*` → `YYYY-MM-DD HH:MM:SS Z` (trước hiển thị wall-clock server verbatim; đã thử local rồi user chốt UTC). e2e message-actions cập nhật assertion `... 10:22:14 Z` + selector copy `[title="Copy full payload"]` (title đã đổi từ trước, test cũ stale).
- **1 Key browser vào ObjectExplorer, KHÔNG mở tab khi connect**: component mới `explorer/RedisExplorer.svelte` (sticky header DB dropdown + SCAN count + ＋Key + Pub/Sub + Flush + pattern filter + cây key theo prefix `:`). ObjectExplorer branch `{:else if isRedis}` (khi connected) render nó. ConnectionList.openOrToggle + CommandPalette.openConn: bỏ `tabs.openRedisTab` cho redis (chỉ select → explorer hiện keys). RedisWorkspace cũ (+CLI console) giờ orphaned (giữ code, không ai mở — cân nhắc bỏ CLI về sau).
- **2 Redis Grid View JSON + Copy cột Data**: click key → tab mới `redis-key` (`workspace/RedisKeyView.svelte`, `tabs.openRedisKey(connId,key)` 1 tab/(conn,key)). Mỗi value có nút **⛶ View JSON** (popup pretty-print + syntax color qua `highlightJson`/`jsonTokenColor` reuse của NATS; fallback raw nếu không phải JSON) + **⧉ Copy** (copy full value). string/hash/zset/list/set/stream đều có.
- **3 Set TTL từng key** + **4 Add key mới (data + TTL)**: RedisKeyView header có **Set TTL** (prompt EXPIRE/PERSIST) + **Delete** + **+Add** item; RedisExplorer **＋ Key** dialog (type string/hash/list/set/zset + name + data textarea theo type + TTL giây) → `redisEdit` (setString/hSet/rPush/sAdd/zAdd loop) + `redisSetTtl` (không cần backend mới — các op tự tạo key). e2e `redis-explorer.spec.ts` viết lại theo flow mới (key browser sidebar + click key mở viewer + View JSON/Copy/Set TTL/Delete + Add-key dialog + Pub/Sub). Gates: check 0/0, vitest 360, tokens 170 (0 mới), e2e redis-explorer 2/2 + message-actions 1/1. **NOTE pre-existing fail (không do task này, fail cả trên baseline sạch)**: kafka-workspace + nats-workspace specs (JetStream consumer / cluster overview).
- **AUDIT-13b — Redis viewer UX (3 mục user)**: (1) **bỏ nút +Add** trong RedisKeyView (gỡ `addItem`); **Set TTL mở FORM** (modal input giây + Save/Cancel, thay `window.prompt` không đáng tin trong webview). (2) **Highlight key đang chọn** trong RedisExplorer: `activeKey` = key của tab `redis-key` đang active (this conn) → row background `color-mix(var(--primary) 26%)`. (3) **Delete = popup confirm in-app** (thay `window.confirm`) + sau khi xóa **refresh key list trong Explorer**: `explorer.redisTick[connId]` counter + `explorer.bumpRedis()` (gọi sau delete/add-item/TTL); RedisExplorer `$effect` watch tick → `load()`. e2e redis-explorer mở rộng: no "+ Add", Set TTL → dialog input, Delete → confirm "Delete key" + Cancel. Gates: check 0/0, tokens 170 (0 mới), e2e redis-explorer 2/2.
- **AUDIT-13c — rule chung popup + đóng app xoá tabs (2 mục user)**:
  - **Click ra ngoài KHÔNG tắt form (rule chung mọi popup dạng form/dialog)**: gỡ `onclick` đóng khỏi backdrop `role="presentation"` (nền dim `rgba(0,0,0,.5)`/`var(--rgba-0-0-0-_5)`) ở TẤT CẢ dialog: Backup/ClickHouseCreate/ClickHouseTtl/CopyTable/ExecuteRoutine/Export/GenerateScripts/GenerateTestData/Import/Settings/NewDatabase/DeleteConnection/EditConnected/SaveBeforeClose/CommandPalette + SchemaCompare(selDiff)/ResultPanel(rawError)/NatsSubjectMessages(confirm+view)/RedisKeyView(view+ttl+confirm)/RedisExplorer(addKey). Giữ Escape (onkeydown) nơi có + nút ×/Cancel. **KHÔNG áp cho** overlay menu/dropdown trong suốt (ResultGrid ctxMenu, SqlWorkspace conn/db drop) — đó là menu, click ngoài vẫn đóng. (ConnectionForm/SystemPicker đã đúng từ AUDIT-6.)
  - **Đóng app = đóng hết tabs**: `tabs.restore()` khi `IS_TAURI` → KHÔNG restore, bắt đầu sạch + `saveTabs([])`/clear `active_tab` (tabs không sống qua restart). Browser/demo giữ nguyên seed DEMO_TABS (để e2e/preview không rỗng). e2e redis-explorer + backdrop-click-không-đóng assertion (`mouse.click(8,8)` → dialog vẫn hiện → đóng bằng Cancel). Gates: check 0/0, vitest 360, e2e full: 80 passed (2 fail kafka-workspace/nats-workspace = pre-existing, fail cả trên baseline sạch).
- **AUDIT-13d — NATS Explorer confirm popup cho Clear/Delete (3 mục user)**: thay `window.confirm` bằng confirm popup in-app trong ObjectExplorer (`confirmState`/`askConfirm`/`runConfirm` + modal `.cfm-btn`; backdrop KHÔNG confirm/đóng — chỉ nút Cancel/Confirm/Escape). (1) **Clear messages** (subject) → confirm; confirm → `natsJsPurgeSubject` + `refreshStreaming` + **`explorer.bumpNatsSubject(conn,stream,subject)`** → NatsSubjectMessages `$effect` watch `natsMsgTick[conn:stream:subject]` → reload (tab focus hiện rỗng). (2) **Delete subject** → confirm riêng (`natsJsRemoveSubject`, giữ stream) — body KHÔNG nhắc "drops the stream", TÁCH BIỆT hoàn toàn với Delete stream. (3) **Delete stream** → confirm (`natsJsDeleteStream`). Kafka giữ window.confirm (ngoài scope). e2e streaming-explorer: delete-stream/delete-subject dùng in-app dialog (assert body khác nhau), +test clear-messages (confirm popup + backdrop-không-đóng + Confirm refresh tab, dùng `getByRole('menuitem')` tránh đụng nút Clear ở header tab). Gates: check 0/0, e2e streaming-explorer 6/6 + message-actions 1/1, tokens 170 (0 mới).
- **AUDIT-13e — NATS Explorer: filter subject theo stream + Add subject/message (3 mục user)**:
  - **Filter subjects theo từng stream** (SSMS-style, KHÁC filter stream-name toàn cục ở đầu cây): stream context menu thêm **"Filter subjects…"** → hiện ô input depth-1 dưới stream (auto-expand + autofocus) lọc subjects bằng `objectFilterMatch`; **"Clear filter"** (menu) + nút × xoá. State `subjFilters`/`subjFilterOpen` (per-stream) trong ObjectExplorer.
  - **Add subject mới** (stream ctx menu → "Add subject…") + **Add message** (subject ctx menu → "Add message…" và nút **＋ Add** trong NatsSubjectMessages grid): dùng chung `natsAddWizard` store + `NatsAddMessageDialog.svelte` (mount App.svelte) — form Subject + Payload + Time(local, mặc định localize, hiển-thị-only vì NATS set publish time ở server). Publish → `ipc.natsJsAddSubject` → `refreshStreaming` + `bumpNatsSubject` (tab đang mở reload). Backdrop KHÔNG đóng (rule chung), Escape/Cancel đóng.
  - **Backend**: `NatsDriver::js_add_subject(stream,subject,payload)` — thêm subject vào stream config nếu thiếu (best-effort, bỏ qua lỗi overlap wildcard) rồi JetStream `publish().await` chờ ack; command `nats_js_add_subject` + register lib.rs + ipc + demo. `cargo build --lib` EXIT=0.
  - e2e streaming-explorer +2 (filter narrows subjects + Clear + Add subject dialog backdrop-không-đóng + Publish đóng; grid ＋ Add prefill subject). Gates: check 0/0, e2e streaming-explorer 8/8 + message-actions 1/1, tokens 170 (0 mới), rust lib build OK.
- **AUDIT-13f — Toolbar View ER / Generate Scripts gate theo schema selected**: 2 nút toolbar Connections (ER + DDL) trước là stub `phase5` + enable theo `selRel` (chỉ cần chọn connection). Giờ **enable CHỈ khi chọn node schema/database (public/dbo/databaseName) trong ObjectExplorer**; chọn table/folder/leaf → disable. Cơ chế: `explorer.selectedSchema` ({connId, base, system, schema}) + ObjectExplorer `erTarget` derive từ `treeSel` (key `s:<schema>` | `curdb` → schema đầu | `fdb:<db>:s:<schema>`/`fdb:<db>` foreign-db sub-connection) + `$effect` push vào store; reset `treeSel=null` khi đổi connection (nút disable lại). ConnectionList `schemaSel` (guard `selectedSchema.base===selConn.id`) → nút ER `tabs.openErDiagram(connId, schema)`, nút DDL `scriptsWizard.show(connId, schema)` (đúng database/sub-connection). Bỏ `phase5`. **Fix test fragility**: `getByTitle('Database')` (substring) đụng title disabled mới ("…select a database…") → đổi audit-hotkeys/audit5-ui sang `getByTitle('Database',{exact:true})` (chip title đúng "Database"). e2e `connection-toolbar.spec.ts` (disabled→chọn public→enable→ER tab `ER · public`→scripts popup). Gates: check 0/0, tokens 170 (0 mới), e2e full 84 passed (2 fail kafka/nats-workspace = pre-existing).
- **AUDIT-13g — MySQL "Illegal mix of collations" khi CALL — CHẨN ĐOÁN, KHÔNG có client fix** (user: `CALL ismart-eco.sp_getSequence('nats')` → HY000 `utf8mb4_0900_ai_ci` vs `utf8mb4_general_ci`): **Đã reproduce chính xác trên container MySQL 8** (diagnostic tạm, đã gỡ): lỗi phát sinh khi BODY của proc so sánh HAI CỘT khác collation (`ON s.name = l.name`, s.name=0900 vs l.name=general_ci) — **cả hai operand IMPLICIT (coercibility 2)**. **Chứng minh KHÔNG session-setting nào fix được**: đặt `collation_connection=utf8mb4_0900_ai_ci` VÀ `SET NAMES utf8mb4 COLLATE utf8mb4_0900_ai_ci` đều VẪN lỗi (collation của cột là cố định server-side, không đổi theo connection). Param-vs-cột (routine IN param vs column khác collation) thì KHÔNG lỗi → lỗi này chắc chắn là cột-vs-cột (hoặc cột-vs-local-var baked). **Đây là schema inconsistency phía server, fail ở MỌI client (DataGrip/Workbench/CLI)** — sửa bằng `ALTER TABLE … CONVERT TO CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci` để đồng nhất collation, hoặc thêm `COLLATE` tường minh trong body proc. **KHÔNG code fix** — đã REVERT thử-nghiệm `SET collation_connection` trong `mysql.rs::connect` (kỷ luật: không ship fix chưa verify). Bug introspection collation (information_schema) đã fix riêng bằng CONVERT từ trước, vẫn giữ.
  - **Diagnostic dứt điểm** (container MySQL 8, đã gỡ test): sqlx default `collation_connection = utf8mb4_unicode_ci` (KHÔNG phải general_ci) → operand lỗi (`0900` + `general_ci`) KHÔNG phải của connection ta → 100% server-side. Kết quả 3 case với proc trên DB `eco`(general_ci) + cột `0900`: **param-vs-cột = OK** (param coerce được); **uservar-vs-cột = FAIL** (`@nm` = collation_connection BAKED lúc CREATE = unicode_ci, không đổi theo cc caller); **localvar-vs-cột = FAIL** (`DECLARE v` = collation mặc định của DATABASE = general_ci, baked). Đặt `SET collation_connection` general_ci HAY 0900 trước CALL đều KHÔNG fix (collation nội bộ routine cố định lúc CREATE + collation cột cố định). ⇒ Chỉ fix được server-side: (a) `ALTER TABLE … CONVERT TO … COLLATE <đồng nhất>`, (b) `ALTER DATABASE` + DROP/CREATE lại routine dưới collation khớp, (c) thêm `COLLATE` tường minh trong body. Cần `SHOW CREATE PROCEDURE` để chỉ ra cột/biến cụ thể.

## PARTITIONS — table partitioning (View + Create + Manage + Convert) cho relational
Yêu cầu người dùng: "Database Relational đã có partition chưa? nếu chưa implement cho MySQL/MSSQL/Postgres/ClickHouse/MariaDB/Cassandra". Trước đó CHỈ ClickHouse (introspect partition_key + ctx menu snippet) + Cassandra (partition key trong PK). PG/MySQL/MariaDB/MSSQL = 0. Đã implement đầy đủ:

- **Backend introspection** — type `PartitionInfo {name, method, key?, expression?, rows?, position?}` (drivers/types.rs) + `LiveConnection::partitions(schema,table)` dispatch + per-driver:
  - PG: `pg_partitioned_table` + `pg_inherits` → child partitions + `pg_get_partkeydef` + `pg_get_expr(relpartbound)`.
  - MySQL/MariaDB: `information_schema.PARTITIONS` (GROUP BY partition, fold subpartitions, `text()`/`text_opt()` cho binary-charset).
  - MSSQL: `sys.partition_functions`/`sys.partition_schemes`/`sys.partitions`/`sys.partition_range_values` → "Partition N" + boundary + key column.
  - ClickHouse: `system.parts` (active) GROUP BY partition + `partition_key` từ system.tables; rỗng nếu không PARTITION BY.
  - Cassandra: partition-key columns (system_schema.columns kind='partition_key') → 1 info row "Partition key: (…)".
  - SQLite/Redis/Kafka/NATS → rỗng. Command `list_partitions` (schema.rs) + register lib.rs + ipc + demo (enrollments = RANGE-partitioned demo).
- **Explorer View** — TableDetail.partitions (loadTableDetail Promise.allSettled +listPartitions) + node "Partitions" (glyph ▤ màu cyan `C.part`) dưới mỗi bảng (main loop + foreign-db subtree); leaf = từng partition (meta = bound/method + rows), right-click = maintenance ops.
- **Manage (per-partition ctx menu)** — pure `sql/partitions.ts::partitionOps(system,schema,table,part)`: PG detach/truncate/drop (act on child table); MySQL/MariaDB truncate/rebuild/analyze/optimize + drop (RANGE/LIST only); MSSQL truncate WITH(PARTITIONS(n)); ClickHouse detach/attach/freeze/drop (dùng partition value). Mở SQL tab review.
- **Create (Table Designer → Partitioning tab)** — `buildPartitionCreate(system,schema,table,spec,keyType)`: PG `PARTITION BY` + child `CREATE TABLE … PARTITION OF` (+HASH modulus/remainder); MySQL inline `PARTITION BY … (defs)` / `PARTITIONS n`; ClickHouse `PARTITION BY expr` (giữa ENGINE và ORDER BY); MSSQL `CREATE PARTITION FUNCTION`+`SCHEME` (pre) + `ON scheme(col)`. Tab chỉ hiện cho engine supportsPartitioning. UI: strategy RANGE/LIST/HASH + key + COLUMNS mode (MySQL) + partition rows (PG RANGE = **From/To** structured; khác = single bound) + **inline live script preview** (script + UI cùng chỗ, syntax-highlighted).
- **Add partition (existing partitioned table)** — Designer tab (existing table seed strategy/key read-only + rows ●, add mới) + **AddPartitionDialog** (ctx menu Partitions ▸ Add Partition… → dialog structured form + live script + Run/Open-in-SQL-tab; `addPartitionWizard` store, mount App.svelte). `buildAddPartition`: PG `CREATE TABLE … PARTITION OF`, MySQL `ALTER TABLE … ADD PARTITION`, MSSQL/CH → warning.
- **Convert existing non-partitioned → partitioned** (Design Table, user yêu cầu "cho phép add partition khi Design table đã có sẵn") — `buildConvertToPartitioned` + `canConvertToPartitioned`: MySQL/MariaDB **in-place** `ALTER TABLE … PARTITION BY …`; PG **recreate** (rename→CREATE LIKE INCLUDING DEFAULTS partitioned→children→INSERT SELECT→DROP old, cảnh báo re-add index/constraint); MSSQL function+scheme+`CREATE CLUSTERED INDEX ON scheme`; ClickHouse → warning (recreate via New Table). Designer: checkbox "Partition this table" enable cho existing non-partitioned (canConvert) → form editable + "Convert-to-partitioned script" preview + warnings inline; buildTableDdl ALTER path phân nhánh convert vs add.
- **Tests**: unit `partitions.test.ts` (29: ops/create/addPartition/convert/parse/keyCols/templates per dialect) + `table-designer.test.ts` (partition weave new-table 4 dialect). Integration container thật `tests/partitions_integration.rs` (target riêng — drivers_integration.rs đang vỡ compile do WIP NATS/Kafka chưa liên quan): introspect PG/MySQL/CH/MSSQL + add-partition PG/MySQL + convert PG/MySQL/MSSQL — TẤT CẢ EXIT=0 (chạy CHÍNH XÁC DDL builder sinh, verify catalog + preserve data). e2e `partitions.spec.ts` (Explorer node + manage menu + Add Partition dialog) + `table-designer.spec.ts` (Partitioning tab new-table + existing add + convert). Gates: check 0/0, vitest 415, tokens 0 mới.
- **Autocomplete alias + duplicate-column** (cùng đợt, task 1): `sql/aliases.ts` (parseTableRefs/resolveRef — FROM/JOIN alias, schema-qual, comma list, comment strip) + async `columnSource` trong SqlWorkspace (lazy-load cột bảng trong FROM/JOIN → suggest `alias.`/`table.`; lang-sql chỉ resolve alias khi cột đã nạp). ResultGrid: dup column name → key `{#each}` theo index (hết crash `each_key_duplicate`) + toast cảnh báo (rows là object keyed theo tên → chỉ giữ 1 giá trị/tên, nhắc alias). Unit aliases.test.ts (14).
- **CÒN LẠI**: Cassandra partition = partition-key (không range/list — bản chất khác, chỉ View); ClickHouse convert existing = phải recreate thủ công (New Table); PG convert copy columns+defaults, KHÔNG tự carry index/constraint/FK (đã cảnh báo).

## RESERVED-WORD AUTOCOMPLETE QUOTING (Query Editor) — đầy đủ mọi hệ relational
Yêu cầu người dùng: khi suggest trong Query Editor, nếu tên table/column **trùng keyword mặc định** của hệ đó thì Tab/Enter phải chèn kèm ký tự quote đúng dialect. Ví dụ MySQL gõ `select * from schedule` → `select * from `schedule``. Mapping: PostgreSQL/SQLite `"…"` · MySQL/MariaDB/ClickHouse `` `…` `` · MSSQL `[…]`.
- **Cơ chế đã có** (từ trước): `sql/reserved.ts::quoteIfReserved(system,name)` (dùng `dialect.ts::quoteIdent`) + `SqlWorkspace.identOption` gắn `apply` (dạng đã quote) vào completion `self`/children của lang-sql; lang-sql giữ nguyên `apply` ở nhánh non-quoted (chỉ strip khi user đang gõ trong dấu quote). Boost table 150 / column 200 để thắng keyword completion cùng tên.
- **GAP đã sửa**: danh sách reserved cũ là subset curated → **thiếu `schedule`** (và nhiều keyword khác). Nay `reservedSet(system)` = `CORE ∪ BY_SYSTEM[system] ∪ LANG_BY_SYSTEM[system]` **trừ SAFE**:
  - `LANG_BY_SYSTEM` = keyword list THẬT của `@codemirror/lang-sql` đọc runtime qua `dialect.spec.keywords` (PostgreSQL/MySQL/MSSQL/SQLite) — bao phủ đầy đủ, khớp chính bộ highlight của editor; MySQL list có `schedule`. ClickHouse/Cassandra không có dialect lang-sql → chỉ CORE+BY_SYSTEM.
  - `SAFE` = ~45 tên cột cực phổ biến là NON-reserved keyword ở vài dialect nhưng hợp lệ khi để trần (`id`,`name`,`value`,`type`,`status`,`code`,`count`,`level`,`state`,`comment`,`data`,`date`,`time`,`timestamp`,`text`,`number`,`description`,`title`,…) → KHÔNG tự quote (tránh `"id"` gây nhiễu). Không tên nào trong SAFE là reserved thật ở engine nào ta hỗ trợ nên để trần không vỡ câu lệnh.
  - Kết quả: `schedule`→`` `schedule` `` (MySQL/MariaDB) & để trần cho PG/MSSQL/SQLite (không phải keyword của chúng); `order`/`user`/`key`/`desc`→quote đúng ký tự từng dialect; `id`/`name`/`value`→để trần.
- **Demo**: thêm 2 bảng reserved-name vào `list_tables` — `schedule` (keyword chỉ MySQL) + `order` (reserved mọi hệ) để e2e kiểm thật.
- **Tests**: unit `reserved.test.ts` (14 — thêm mapping ký tự theo bảng, `schedule` chỉ MySQL/MariaDB, coverage lang-sql keyword đầy đủ, SAFE giữ trần). e2e `editor-autocomplete.spec.ts` (MySQL: `sched`→`` `schedule` ``, `ord`→`` `order` ``; PG: `ord`→`"order"`, `sched`→trần — mở tab đúng connection qua ctx "New Query Console"). **Integration container thật** `drivers_integration.rs`: `{sqlite,pg,mysql,mssql}_reserved_identifier_quoting_roundtrip` — tạo bảng `order`(cột `select`) + `schedule`(MySQL) bằng CHÍNH ký tự quote frontend sinh, INSERT + SELECT round-trip → EXIT=0 cả 4 (chứng minh identifier đã quote là SQL hợp lệ engine chấp nhận).
- **Sửa kèm (bắt buộc để build được test)**: `drivers_integration.rs` trước đó vỡ compile (call-site cũ) — cập nhật `js_subject_messages(…, None)` (thêm `start_seq` từ đợt NATS pagination) + `browse_consumer(…, DefaultConsumerContext)` (thêm `context` từ đợt Kafka). Nay integration binary compile lại được.
- Gates: check 0/0, vitest 448, e2e editor-autocomplete 4/4, integration reserved-quoting {sqlite,pg,mysql,mssql} EXIT=0, tokens 0 mới.

## FIX — TableDesigner column reorder = drag&drop THẬT (pointer events)
Người dùng: kéo-thả sắp xếp cột trong TableDesigner không hoạt động (chỉ có nút ▲▼). Root cause: reorder trước dùng **native HTML5 drag** (`draggable`/`ondragstart`/`ondrop`) — trong Tauri WebView2, trình xử lý drag-drop cấp OS (file-drop) NUỐT sự kiện dragstart/drop trong trang → DnD native im lặng không chạy (browser/Playwright `dragTo` vẫn "pass" vì mô phỏng DnD API, che mất lỗi thật). Fix: chuyển sang **pointer events** — `onpointerdown` trên ô `#` → `dragStart` gắn listener `window` pointermove/up (bền hơn pointer-capture), **live reorder** theo `rowIndexFromY` (hàng con trỏ đang phủ, containment — thả vào giữa hàng là đổi chỗ), `onDestroy` gỡ listener. `{#each cols (col)}` key theo object nên reorder di chuyển DOM (giữ focus/giá trị input). Nút ▲▼ giữ làm fallback (span `onpointerdown` stopPropagation để không kích hoạt kéo). Thứ tự hàng = thứ tự cột `buildTableDdl` phát khi Save (đã có). e2e `table-designer.spec.ts` đổi bước drag từ `dragTo` → `page.mouse.down/move(steps)/up` (drive pointer thật); vẫn assert order vào DDL + ▲▼. Gates: check 0/0, unit table-designer 38, e2e table-designer 7/7, tokens 0 mới.

## FIX — TableDesigner drag handle icon + hướng kéo (up/down) + Ctrl+S giữ thứ tự
Người dùng: đổi icon Drag&Drop (`▲ 8 ▼`) sang icon nhìn-là-biết-kéo, khi giữ chuột kéo phải thể hiện đang kéo LÊN hay XUỐNG; Ctrl/Cmd+S lưu giá trị + thứ tự đã sắp. Ô `#` giờ: **grip ⠿** (SVG 2 cột × 3 chấm — affordance "kéo tôi" chuẩn) + số thứ tự + ▲▼ nhỏ (fallback). Khi đang kéo: grip biến thành **badge hướng ↑/↓** (accent, `↕` lúc mới cầm) theo `dragDir` = so `dragRow` với `dragStartRow`; hàng được kéo **nhấc lên** (nền accent-tint + thanh accent trái + box-shadow) + số in đậm accent; cursor `grabbing` toàn cửa sổ (`document.body.style.cursor`, nhả ở pointerup/onDestroy). Ctrl/Cmd+S đã lưu đúng thứ tự (buildTableDdl phát theo mảng cols) — không đổi. Gates: check 0/0, e2e table-designer 7/7, tokens 0 mới.

## OBJECTS TAB — tab "Objects" (pinned, singleton) khi double-click database name
Người dùng: double-click database name trong Explorer → NGOÀI expand/collapse (giữ nguyên), mở thêm tab "Objects" pinned/singleton ở index 0 liệt kê tables (Table Name · Data Length · Rows). **Additive, KHÔNG refactor tab system.**
- **Backend (reuse `list_tables`, không query trùng):** thêm `TableInfo.data_length: Option<i64>` (types.rs) — MySQL/MariaDB `CAST(COALESCE(DATA_LENGTH,0)+COALESCE(INDEX_LENGTH,0) AS SIGNED)` (**bonus: CAST row_estimate AS SIGNED** — information_schema numeric = BIGINT UNSIGNED → `try_get::<i64>` xưa nay trả None → MySQL row_estimate luôn rỗng; giờ có số đúng như screenshot), PG `pg_total_relation_size(oid)`, MSSQL `SUM(reserved_page_count)*8192` (sys.dm_db_partition_stats), ClickHouse `system.tables.total_bytes`, SQLite `None`.
- **Tab type mới `'objects'`** (types.ts) + `ObjectsView.svelte` (grid 3 cột; empty state khi disconnect/xoá; refresh khi đổi DB; **dup-guard** `loadedKey` = `connId::db::schema` → double-click cùng DB không refetch; lấy tables qua `ipc.listTables`, schema=null → gộp mọi schema; chỉ hiện `kind==='table'`; `formatBytes` pure trong `format/bytes.ts`).
- **tabs store `openObjectsTab({connId,database,schema})`**: singleton (retarget state + activate nếu đã có, KHÔNG tạo tab 2), `unshift` index 0, `isPinned:true`. Guards: `isClosable()` (contentType!=='objects'); `requestClose`/`forceClose` lọc bỏ objects; `reorder` chặn kéo objects + clamp drop-đích ≥1 (giữ index 0); `duplicate` bỏ qua; `restore` đưa objects về front.
- **TabBar**: objects = không `draggable`, không middle-click close, không nút ×, không rename (dblclick), context menu rút gọn ("Objects (pinned)" disabled). Close All/Others/Right tự skip (isPinned + filter).
- **ObjectExplorer**: `onDblClick` trên node database (MySQL/MariaDB/ClickHouse schema-as-db → `openObjectsForSchemaDb` = expandSchema + Objects; PG/MSSQL current-db header → `openObjectsForCurrentDb`; foreign-db node → `openObjectsForForeignDb` = await toggleForeignDb (attach) + Objects trên sub-connection). Expand/collapse cũ giữ nguyên (onClick chevron + onDblClick vẫn gọi toggle).
- **Tests**: unit `format/bytes.test.ts` (3) + `tabs.test.ts` mở rộng (singleton/pinned-index-0/không-close [requestClose+forceClose+Close All/Others]/không-drag-drop-before/không-duplicate/restore-giữ-index-0); e2e `objects-tab.spec.ts` (2: double-click mở Objects index-0 + 3 cột + data-length "1.1 MB" + rows "3,842" + views bị loại + không nút × + regression expand vẫn tăng "public"; double-click DB khác refresh cùng tab, vẫn 1 Objects). Integration data_length: pg_full_introspection + mysql_explorer_tree + mssql_roundtrip + clickhouse_roundtrip đều assert `data_length.is_some_and(>0)` EXIT=0.
- Gates: check 0/0, vitest 459, playwright objects-tab 2/2 (+ full suite; chỉ kafka/nats-workspace fail = pre-existing baseline), Rust unit 83, integration pg/mysql/mssql/clickhouse EXIT=0, tokens 0 mới.

## OBJECTS TAB — nâng cấp UX + context menu dùng chung (rule chung)
Người dùng: Objects tab thêm cột `#`, selected value màu xanh dương + hover; context menu ĐẦY ĐỦ giống context menu tableName ở ObjectExplorer cho mọi relational (**rule chung**); phân bổ cột hợp lý + đổi background dễ nhìn.
- **`TableContextMenu.svelte` (single source = rule chung)**: tách toàn bộ menu bảng quan hệ (Open Data / Import / Export / Copy to / Generate Test Data / New Query / Design Table / Alter Table / Manage Indexes & FKs / Partitions [Show*+Add] / Rename / Generate SQL SELECT·INSERT·UPDATE·DELETE / Generate Scripts [structure·data·both] / View DDL / ClickHouse ops [TTL·Optimize·Partitions·Engine·Mutations·FINAL·Detach·Freeze·Drop partition] / Copy Name·Qualified·DDL / Refresh / Truncate·Drop) thành 1 component render `<ContextMenu.Content>`. Self-contained (import wizards + tabs + explorer + ddl/generate/chops/dialect; tự sinh SQL qua các pure gen đã unit-tested); 2 hành vi phụ-thuộc-ngữ-cảnh là callback: `onShowPartitions` (cây mở node Partitions; Objects tab bỏ item này) + `onRefresh` (mặc định `explorer.refresh`). Props: connId/schema/table/system/locked/engine/database.
- **ObjectExplorer**: `tableMenu` chính (bảng của current-db) giờ render `<TableContextMenu … onShowPartitions=showPartitions onRefresh=explorer.refresh />` → **1 nguồn dùng chung** với Objects tab (e2e cây validate parity: audit-fixes #4, table-designer, index-manager, generate-scripts, partitions, audit5-ui đều xanh). fObjMenu (foreign-db, mixed-kind) giữ nguyên (menu object đa-loại, ngoài phạm vi bảng).
- **ObjectsView**: chuyển `<table>` → div-grid `# | Table Name(1fr) | Data Length(160) | Rows(110)` (ARIA role table/row/columnheader/cell để accessible + e2e locate). Cột `#` (số thứ tự, phải, muted). Row **select màu xanh** (`color-mix(--primary 28%)`, aria-selected) khi click; **hover** `var(--hover)`; **zebra** hàng lẻ `var(--surface)`; nền view `var(--bg)`. Double-click row → Open Data (openTableViewer). Mỗi row bọc `ContextMenu.Root` + `<TableContextMenu connId schema table system database(=schema cho mysql/mariadb/clickhouse; undefined cho pg/mssql vì connId đã trỏ đúng DB) onRefresh=reload />`.
- **Tests**: unit `format/bytes.test.ts` + tabs Objects-singleton (đã có); e2e `objects-tab.spec.ts` mở rộng: `#` columnheader + click row → aria-selected=true + right-click → menu đủ item (Open Data/Design Table/Generate SQL·SELECT/Manage Indexes/Copy DDL/Truncate/Drop). Context-menu actions = pure gen đã unit-tested + e2e; **không đổi backend** nên integration data_length (pg/mysql/mssql/clickhouse EXIT=0) giữ nguyên.
- Gates: check 0/0, vitest 459, playwright 24/24 (objects-tab + toàn bộ spec menu-cây), tokens 0 mới.

## OBJECTS TAB — Refresh hoạt động thật + feedback (rule chung cho Refresh)
Người dùng hỏi nút Refresh ở Objects tab đã chạy chưa. **Chẩn đoán**: đã re-query backend (`refresh`→`load`→`ipc.listTables`, list_tables luôn query catalog mới, KHÔNG cache) NHƯNG 2 khiếm khuyết khiến trông như không chạy: (1) **không có feedback** — reload xong dữ liệu giống hệt (nhất là MySQL `TABLE_ROWS` là estimate cache của engine) nên nhìn như bất động; (2) **hỏng dup-guard** — `refresh()` set `loadedKey=''` nhưng `load()` không phục hồi → sau 1 lần Refresh, guard "cùng DB không refetch" hỏng vĩnh viễn (re-render sau đó refetch thừa).
- **Rule chung cho Refresh** (áp cho Objects tab, dùng làm chuẩn tham chiếu): (a) LUÔN re-query backend, bỏ qua cache; (b) hiện chỉ báo đang chạy — glyph ⟳ **xoay** (CSS `@keyframes` + `class:spinning`) + đổi chữ "Refresh"→"Refreshing…" + dim + `aria-busy`; (c) **guard double-click** (`if (refreshing) return`, `async/await load`); (d) KHÔNG đụng `loadedKey` (target không đổi → dup-guard còn nguyên).
- **Test observability**: `demoInvoke` (demo/browser only, no-op trong Tauri) đếm số lần gọi mỗi command vào `window.__ipcCalls` để e2e assert Refresh thật sự re-hit backend.
- e2e `objects-tab.spec.ts` +1: click "Refresh objects" → `window.__ipcCalls.list_tables` tăng (chứng minh re-fetch) + grid vẫn render + không page error. Context-menu "Refresh" của row cũng dùng `onRefresh=refresh` (đồng nhất). Không đổi backend → Rust/integration giữ nguyên.
- Gates: check 0/0, vitest 459, playwright objects-tab 3/3, tokens 0 mới.

## OBJECTS TAB — hiển thị dạng table có đường viền
Người dùng: Objects tab phải là table có **đường viền** để phân biệt rõ cột/hàng. Trước đó div-grid chỉ có `border-bottom` mỗi hàng (không có vạch dọc giữa cột). Bổ sung: (a) **outer frame** trên container `role="table"` (`border:var(--px-1) solid var(--border2)` + `margin:var(--px-10)`); (b) **vạch dọc giữa cột** — hằng `SEP=border-right:var(--px-1) solid var(--border)` áp cho 3 cột đầu (# / Table Name / Data Length) ở CẢ header + body (cột Rows cuối để trống, đã có frame phải); (c) giữ `border-bottom` mỗi hàng làm vạch ngang. Sticky header không đặt `overflow:hidden` trên container (tránh vỡ sticky). Chỉ dùng token màu (`--border`/`--border2`) → tokens 0 mới. Gates: check 0/0, playwright objects-tab 3/3.

## OBJECTS TAB — header connection/database + font Result Grid (rule chung font)
Người dùng: bỏ chữ "Objects" trong header (đã trùng tiêu đề tab), thể hiện connection + database rõ + có màu; data trong table dùng **cùng font/size như Result Grid** (kiểu DataGrip); 3 cột rõ, có màu chữ/nền. **Font/size là rule chung cho tất cả.**
- **Header**: bỏ dòng "Objects"; 1 dòng = tên connection (`--text`, 13px, 600) + `/` (`--muted`) + **database in màu accent hệ** (`color:{accent}`, mono 13px, 700) + `title` full để hover. Bên phải giữ "N tables" + Refresh.
- **Rule chung font (Result Grid)**: hằng `GRID_FONT` = `.mono` (JetBrains Mono) + `font-size:var(--px-12)` + `font-variant-numeric:tabular-nums` + `font-feature-settings:'tnum' 1,'zero' 1`; đặt ở container `role="table"` (kế thừa xuống mọi cell) — số thẳng cột như Result Grid. Header cell: padding `var(--px-6) var(--px-12)`, weight 600, `--text2` (khớp `th` Result Grid). Body cell: padding `var(--px-5) var(--px-12)`; `#` gutter 10.5px.
- **Màu cột**: Table Name = `--text` (weight 500); **Data Length + Rows = `--syntax-number`** (màu số, theme-aware) → phân biệt cột số; `#` = `--muted`. Row **selected = `--grid-select`** (xanh dương, ĐỒNG BỘ Result Grid) + chữ trắng (`cellColor(i, base)` đổi sang `--hex-fff` khi selected); hover `--hover`; zebra hàng lẻ `--surface`. Viền khung + vạch dọc `SEP` giữ nguyên.
- e2e `objects-tab.spec.ts`: đổi assert subtitle sang `getByTitle('Postgres / analytics')` / `getByTitle(/Postgres \/ (app|sis_prod)/)` (tách span + separator `/`). Gates: check 0/0, playwright objects-tab 3/3, tokens 0 mới.

## POSTGRES ICON — đổi sang logo cylinder + elephant (SVG)
Người dùng dán ảnh icon Postgres mới (blue database cylinder + PostgreSQL elephant) yêu cầu thay. **Ràng buộc**: không trích được bytes ảnh dán trong chat ra file → không ghi đè trực tiếp `db-postgres.png`. Giải pháp: **tái tạo bằng SVG tự chứa** `public/assets/db-postgres.svg` (cylinder royal-blue 3 dải + elephant periwinkle `#8ea3d4` viền `#3a5cc4` — tai/vòi/mắt), `SystemIcon.svelte` postgres branch trỏ `.png`→`.svg`. Verify: render preview 16/24/48/128px (dark+light) OK; screenshot sidebar app thật thấy icon đúng cạnh nhóm POSTGRESQL. **Pixel-diff không vỡ**: `phase2-regions` vùng sidebar-connections vẫn 0.20% (<2%) dù prototype giữ `.png` cũ (icon 15px khác biệt nhỏ, dưới ngưỡng). `db-postgres.png` cũ để lại (unused, vô hại). Gates: check 0/0, phase2-regions 4/4 vùng ĐẠT. **Fixup (bắt lỗi khi làm task Redis sau)**: `color-identity.test.ts` assert postgres = `db-postgres.png` → cập nhật thành `.svg` (mysql/mssql giữ `.png`); lần đầu quên chạy full vitest sau đổi icon nên sót.

## OBJECT EXPLORER — header Refresh có CHỮ + chạy cho MỌI connection type
Người dùng: nút ⟳ ở header EXPLORER chỉ có icon → thêm chữ "Refresh" cho dễ nhận biết + phải chạy được cho tất cả loại connection. Trước đó header gọi cứng `explorer.refresh(id,{kind:'connection'})` — chỉ đúng cho relational/ClickHouse; **Kafka/NATS/Redis/Cassandra không reload** (chúng dùng path riêng: loadStreaming / bumpRedis / loadCass). Fix: `refreshConnection()` **dispatch theo `selected.system`**: kafka|nats → `explorer.loadStreaming(id, system, true)`; redis → `explorer.bumpRedis(id)` (RedisExplorer reload theo tick); cassandra → `loadCass(id)`; còn lại (pg/mysql/mariadb/mssql/sqlite/clickhouse) → `explorer.refresh(id,{kind:'connection'})` + PG/MSSQL thêm `loadDatabases(id)`. Header giờ hiển thị **"⟳ Refresh"** (glyph + chữ, weight 600) theo **Refresh rule chung**: guard double-click (`refreshingTree`) + glyph xoay + "Refreshing…" + `aria-busy` + dim khi chưa chọn connection. e2e `explorer-refresh.spec.ts` (dùng `window.__ipcCalls`): Postgres → click Refresh → `list_schemas` tăng; Kafka → `kafka_topics` tăng (chứng minh dispatch relational + streaming). Pixel-diff không đổi (header Explorer nằm NGOÀI vùng sidebar-connections). Gates: check 0/0, explorer-refresh 1/1, regression explorer specs 17/17, phase2-regions 0.20%, tokens 0 mới.

## REDIS EXPLORER — context menu Delete + Refresh (chạy thật trên Redis)
Người dùng: RedisExplorer thêm context menu gồm **Delete** (xóa hẳn trên Redis) + **Refresh** (SCAN mới từ Redis). Right-click cả **key leaf** và **folder** (prefix). Refresh = `load()` (SCAN vòng lặp cursor tới hết). Delete qua **confirm in-app** (rule chung — backdrop không đóng): key → `redisDel(key)`; folder → xóa mọi key dưới prefix qua pure `keysUnderPrefix(keys, prefix)` (prefix chính nó + `prefix:*`, KHÔNG dính sibling `userdata` khi prefix=`user`). Sau xóa: reload + `explorer.bumpRedis(connId)` (key-viewer tab đang mở tự reload). `redisDel`→command `redis_del`→`RedisDriver::del` = `DEL`. **Test**: unit `redis/tree.test.ts` +4 (`keysUnderPrefix`: prefix-self+subtree, không dính sibling, nested, unknown→[]); integration `redis_del_removes_key_and_scan_reflects_it` (SET 3 key → DEL 1 → SCAN không còn key đó, sibling còn, DEL lại = 0) EXIT=0; e2e `redis-explorer.spec.ts` +1 (right-click key → menu có Delete+Refresh; Delete → dialog "Delete from Redis" → confirm → `redis_del` gọi + dialog đóng; Refresh → `redis_scan` tăng — qua `window.__ipcCalls`). Gates: check 0/0, vitest 463, e2e redis-explorer 3/3, integration redis_del EXIT=0, tokens 0 mới.

## FIX — Redis Refresh (verify) + NATS Delete stream (root cause) + NatsSubjectMessages grid selection
Ba mục người dùng báo:
1. **Redis header Refresh — ĐÃ hoạt động** (verify, không cần fix): `refreshConnection` nhánh redis → `explorer.bumpRedis(id)` → `redisTick` → RedisExplorer `$effect` reload → `redis_scan` mới. e2e `explorer-refresh.spec.ts` +Redis case (click Refresh → `window.__ipcCalls.redis_scan` tăng) chứng minh re-query thật.
2. **NATS Delete stream — root cause = DEMO** (backend đúng): real backend `js_delete_stream`→`js.delete_stream` xóa thật; **integration `nats_jetstream_streams_consumers_peek` bổ sung assert**: create T9S → có trong `js_streams` → delete → **KHÔNG còn** trong list (trước chỉ delete không assert). Lỗi user thấy "xóa rồi Refresh lại hiện" là do **demo mock** `nats_js_delete_stream` no-op + `nats_js_streams` trả list TĨNH. Fix demo **stateful**: `demoNatsStreams` (module state) + `nats_js_delete_stream` filter bỏ theo name + `nats_js_create_stream` push → delete thật sự biến mất, refresh không thấy lại. e2e `streaming-explorer.spec.ts` +1 (Delete stream → Confirm → ORDERS biến mất, EVENTS còn, Refresh vẫn mất).
3. **NatsSubjectMessages grid — selected record + font Result Grid, KHÔNG che 3 icon**: table `.mono` + `tabular-nums` + `'tnum'/'zero'` (GRID_FONT, như Result Grid); click row → `selSeq` → nền **`--grid-select`** (xanh) + chữ trắng (`cellColor(seq, base)`); hover `--hover`. **3 icon (View JSON ⛶ / Copy ⧉ / Delete ×) đổi sang TRẮNG khi row selected** (cellColor) → không bị nền xanh nuốt; icon `onclick` thêm `stopPropagation` (bấm icon không đổi selection). e2e `message-actions.spec.ts` +1 (click cell → `aria-selected=true` + 3 icon visible + computed color của Copy/Delete = `rgb(255,255,255)`).
- Gates: check 0/0, vitest 463, e2e streaming-explorer 10/10 + message-actions 2/2 + explorer-refresh 1/1 + redis-explorer 3/3, integration `nats_jetstream_streams_consumers_peek` EXIT=0, tokens 0 mới.

## FIX — Redis Flush + MySQL/MSSQL Session syntax error + Result Grid # width (4 mục)
1. **Redis Flush không chạy** → dùng `window.prompt`/`confirm` (không đáng tin trong Tauri webview). Fix: **in-app confirm dialog** (`flushOpen`/`confirmFlush` trong RedisExplorer) → `redisFlushDb` (FLUSHDB) + reload + `bumpRedis`. e2e `redis-explorer.spec.ts` +1 (Flush → dialog "Flush database" → "Flush db0" → `redis_flushdb` gọi qua `__ipcCalls`).
2. **MySQL Session lỗi syntax** near `database` → `db AS database` dùng **reserved word** làm alias không quote. Fix `commands/admin.rs`: `` db AS `database` `` (back-quote). Integration `mysql_admin_sessions_query_runs` (chạy admin_query → Rows có cột `database`, `pid`, `state`) EXIT=0.
3. **MSSQL Session lỗi** "Incorrect syntax near the keyword 'database'" → `DB_NAME(database_id) AS database` cùng lỗi. Fix: `AS [database]` (bracket). Integration `mssql_admin_sessions_query_runs` EXIT=0. Unit `admin_query_per_dialect` +assert alias quoted (`` AS `database` ``/`AS [database]`).
4. **Result Grid cột # rộng quá** → gutter `th`/`td` (+ insert-row `＋`) thêm `width:1%` (+`white-space:nowrap` sẵn có) → shrink-to-fit vừa đủ nội dung, không chiếm width thừa.
- Gates: check 0/0, vitest 463, Rust unit admin:: 3/3, integration `mysql_admin_sessions_query_runs` + `mssql_admin_sessions_query_runs` EXIT=0, e2e redis-explorer 4/4 + result-pager/group-by/result-copy/audit5-ui 9/9, tokens 0 mới.

## FIX — NATS Delete stream phải xóa THẬT trên server (verified)
Người dùng: Delete stream trong NATS Explorer không xóa trên server NATS của connection đang kết nối. Hai cải tiến đảm bảo "xóa thật trên server":
- **Driver `js_delete_stream` verified**: trước bỏ qua kết quả (`js.delete_stream(name)` → `Ok(())` bất kể). Nay đọc `DeleteStatus.success` — nếu server KHÔNG xác nhận (`success=false`) → trả **error** (không báo "deleted" giả). Lỗi permission/JetStream cũng surface rõ (frontend toast error) thay vì im lặng.
- **Frontend deterministic refresh**: `deleteStream` sau khi xóa **await `explorer.loadStreaming(id,'nats',true)`** (thay `refreshStreaming` fire-and-forget) → cây sidebar phản ánh đúng state server ngay.
- **Integration** `nats_delete_stream_persists_on_server_after_reconnect` (nats `-js`): create DELME → delete → **reconnect client MỚI** → `js_streams` KHÔNG còn DELME (chứng minh xóa trên SERVER, không phải chỉ view local). Regression `nats_jetstream_streams_consumers_peek` (delete T9S qua success-check) EXIT=0.
- Gates: check 0/0, e2e streaming-explorer 9/9, integration `nats_delete_stream_persists…` + `nats_jetstream_streams_consumers_peek` EXIT=0, rust lib build OK.

## FIX — Query Editor: suggest columns của bảng trong FROM (kể cả bare, không cần alias.)
Người dùng: chọn column trong table chưa được suggest. Trước đó `columnSource` CHỈ gợi ý cột sau `alias.`/`table.` (có dấu chấm); gõ cột TRẦN trong SELECT/WHERE (không qualifier) KHÔNG có cột nào — chỉ table/keyword/function. Fix `SqlWorkspace.columnSource` thành 2 nhánh (đều resolve theo FROM/JOIN của statement — `parseTableRefs` thấy FROM cả khi nó nằm SAU con trỏ `SELECT ▮ FROM t`):
- **Case 1** (giữ): sau `alias.`/`table.` → cột của đúng bảng đó.
- **Case 2 (MỚI)**: identifier trần → gộp cột của MỌI bảng trong FROM/JOIN (như DataGrip). Chỉ bắn khi có ≥1 bảng FROM; nhiều bảng → detail hiện `alias · type` để biết cột thuộc bảng nào; dedup theo `qualifier.col`. Cột nạp lazy qua helper `colsOf` (cache → trả ngay; chưa có → kick off `loadTableDetail` + trả null, popup refresh ở phím kế). Sync (không trả Promise) để Tab/Enter accept được.
- e2e `editor-autocomplete.spec.ts` +2: `SELECT * FROM students WHERE students.` → `first_name`; bare `SELECT * FROM students WHERE fi`+`r` → `first_name`. Gates: check 0/0, vitest 463, e2e editor-autocomplete 6/6.

## FIX — NATS delete stream: tự kiểm chứng trên server (self-verify)
Người dùng báo delete stream (app desktop thật, NATS chạy Docker, streams tên thật) "chỉ xóa dưới cache" — lên server vẫn còn. Điều tra: luồng frontend + driver ĐÚNG và nhất quán (cùng client/context cho list+delete+refetch), integration đã chứng minh xóa thật trên NATS container. Suy luận chặt: ở app thật, cây chỉ bỏ stream SAU khi đọc lại từ server ⇒ biến mất-trong-app ⟹ server-đã-xóa. Nếu "còn trên server" thì app & lệnh kiểm tra đang nhìn 2 view JetStream khác nhau (account/domain/endpoint) HOẶC stream bị tạo lại bởi config/controller.
- **Thêm self-verify vào `NatsDriver::js_delete_stream`**: sau `delete_stream` (đã check `DeleteStatus.success`), **liệt kê lại bằng CHÍNH client đó** (`js.streams()`); nếu stream VẪN còn → trả lỗi rõ: "Stream '…' still exists after delete — server did not remove it. Likely JetStream domain/account mismatch, or recreated by config/controller." Biến "im lặng xóa-cache" thành lỗi đúng bản chất; frontend đã `toasts.error` sẵn nên hiện thẳng thông báo cho user.
- Happy path không false-positive: `nats_delete_stream_persists_on_server_after_reconnect` + `nats_jetstream_streams_consumers_peek` (đều delete qua path mới) EXIT=0 (re-list xác nhận đã mất → trả Ok). Lib build OK.

## FIX — Kafka qua SSH tunnel: rewrite thêm FindCoordinator (slow load + không có message)
Người dùng: Kafka (KRaft, 1 broker, Docker server) nối qua **SSH key** → topic hiện nhưng chậm + click topic KHÔNG có message. Trace (`DBSTUDIO_KAFKA_TRACE=1`) chứng minh: **Metadata rewrite CHẠY ĐÚNG** (v13 → `127.0.0.1:<tunnel>`), nhưng consumer duyệt (có `group.id`) gọi **FindCoordinator (api_key=10)** → proxy **KHÔNG rewrite** response đó → coordinator trả địa chỉ advertised thật (`10.16.71.3:9092`) → librdkafka dial THẲNG (bỏ qua tunnel, không hiện trong trace) → kẹt tìm coordinator → **không hề gửi Fetch/ListOffsets** → rỗng message + churn kết nối (chậm).
- **Fix `tunnel.rs`**: thêm `API_KEY_FIND_COORDINATOR=10` + `rewrite_findcoordinator_response` (v0-2 classic, v3 flexible, v4 batched `coordinators[]`) rewrite `coordinator host:port` → `127.0.0.1:<tunnel>` giống Metadata; wire vào dispatch response của `proxy_kafka`. Helper `copy_str`/`put_str`/`copy_n`/`copy_tagged` (compact & classic string, tagged fields). Sau khi rewrite → coordinator vòng qua tunnel → consumer qua bước tìm coordinator → Fetch chạy → có message.
- **Kèm giảm chậm**: `KafkaDriver::topics()` watermark timeout 5s→2s/partition (best-effort, không để 1 partition treo cả list).
- **Test**: unit `tunnel::tests` +3 (findcoordinator v2/v3/v4 — rewrite host/port, giữ node_id/key/error, xoá địa chỉ cũ) → `cargo test --lib tunnel::` 7/7. Lib build OK. (Metadata rewrite v12 test cũ vẫn xanh.)
- **Fixup (sau khi message đã hiện):** toast "Global error: PartitionEOF … reached end of partition" là **thông tin, không phải lỗi** (đọc hết partition). Nó tới qua `BrowseContext::error` (callback lỗi của librdkafka, tách biệt với poll loop vốn đã bỏ qua PartitionEOF). Fix: trong `ClientContext::error`, bỏ qua khi `error.rdkafka_error_code() == Some(RDKafkaErrorCode::PartitionEOF)` → không emit kafka-status. Lib build OK.
- **Fixup 2 (load message chậm + toast EOF vẫn hiện):** (a) **Chậm** — trước đây MỖI message emit 1 event Tauri + frontend `messages=[msg,...messages].slice(MAX)` mỗi message ⇒ O(n²) copy + re-render hàng trăm lần. Fix `KafkaConsumer.svelte`: buffer `pending[]` + flush throttled 120ms (1 reactive update/tick, reverse để newest lên đầu); Clear xoá cả pending; clearInterval ở onDestroy. (b) **Toast EOF robust** — `BrowseContext::error` lọc PartitionEOF cả bằng code (`rdkafka_error_code`) LẪN text (`display.contains("PartitionEOF")` / `reason.contains("reached end of partition")`) vì callback bọc dưới `KafkaError::Global` (code có thể không resolve trên mọi version rdkafka). check 0/0, lib build OK. (Cần rebuild backend để nhận cả 2.)

## FEATURE — QueryEditor chỉ chạy khi connection ĐANG MỞ (disconnect → chặn + báo reopen)
Người dùng: QueryEditor chỉ thực thi khi connection của DB đang mở; nếu đã Disconnect thì mọi tab liên quan connection đó KHÔNG chạy được + hiện lỗi "kết nối đã tắt, vui lòng mở lại"; Open Connection lại → query bình thường. **Additive — không xoá tính năng cũ.** Trước đây `results.run` **tự động reconnect** khi `!connected` (base case) → disconnect rồi run vẫn âm thầm mở lại (trái yêu cầu); path per-tab (`{base}#tab-…`) thì `resolveRunConn`→`open_tab_connection`→`registry.connect` cũng mở connection MỚI từ storage bất kể base connected → im lặng reconnect.
- **`results.run` (central choke point)**: thay block auto-connect bằng guard `if (!profile.connected)` (profile = base qua `baseOf(connId)`; disconnect quét cả `{id}#…`/`{id}::…` nên base.connected=false đại diện cho mọi derived id) → `toasts.error('Connection "…" is closed. Please open the connection again.')` + return, KHÔNG seed execution state.
- **`SqlWorkspace` helper `ensureConnected()`**: gọi TRƯỚC `resolveRunConn()` trong `run()` + `runAtCursor()` để chặn `open_tab_connection` mở connection nền im lặng khi base đã disconnect (isOrphan → "reassign first"; `!profile.connected` → cùng thông điệp reopen). Banner "Disconnected"+Reconnect (đã có từ SPEC_v2 §12) là đường mở lại; SQLite runSql path đã gate sẵn `profile?.connected`.
- **Test**: unit `results.test.ts` (4 — closed→chặn+toast reopen+no exec state; derived id `c1#tab-…` base disconnected→chặn; reopened→exec 1 lần no toast; no profile→"Tab has no connection"). Gates: check 0/0, vitest 477, e2e query-run (result-pager/group-by/editor-autocomplete/result-copy 9/9) xanh (demo connections connected=true nên không đụng guard).

## CASSANDRA — hoàn thiện tính năng cho developer (Phase C1–C5, chỉ Cassandra, additive)
Sau review phát hiện backend Cassandra chạy thật nhưng nhiều năng lực không được frontend gọi. Đã bổ sung, **khoanh vùng tuyệt đối trong nhánh Cassandra** (chỉ thêm match-arm/hàm/component mới; field mới trên type dùng chung là optional). Không đụng DB khác — regression PG/grid + `cassandra_cql_semantics_paging_and_ddl_roundtrip` xanh.
- **C1 — CQL editor qua `cql_exec` thật (paging + warnings)**: `results.run` nhánh `system==='cassandra'` → `ipc.cqlExec` (thay `execStatement`); `SubResult.cqlNextPage/cqlConsistency` (optional), warnings ALLOW FILTERING → Messages + toast; `results.fetchMoreCql` + nút "Load next page" (ResultPanel `onLoadMore`). Trước đây lệnh `cql_exec` là dead code (editor chốt 500 dòng, mất warnings).
- **C2 — Explorer đa-keyspace + View DDL mọi object**: `loadCass` liệt kê MỌI keyspace (lazy `cassandra_tree` per keyspace, cache); menu New Query/Create/Drop/Refresh. Backend pure `format_{type,index,view,function,aggregate}_ddl` + `object_ddl(keyspace,kind,name)` (dựng lại từ `system_schema.*`; index query theo keyspace rồi lọc name vì PK=(ks,table,index)) + command `cassandra_object_ddl`. Trước chỉ hiện 1 keyspace + DDL chỉ CREATE TABLE.
- **C3 — Editable data grid**: reuse `ResultGrid` NGUYÊN TRẠNG. Backend pure `cql_change_sql(&GridChange)` (INSERT/UPDATE/DELETE by full PK, literal theo type: uuid/số/bool/blob bare, text/timestamp quoted) + `apply_grid` (mod.rs arm Cassandra thay lỗi cũ) + `preview_sql` arm `"cassandra"` (grid.rs) + command `cassandra_columns`. FE: tab `cassandra-table` + `tabs.openCassandraTable` + `CassandraTableView.svelte` (load qua cqlExec paging + pkCols từ cassandra_columns + WHERE partition-key optional); Explorer "Open Data (editable)" + double-click.
- **C4 — Consistency per-statement**: `exec_cql_c(cql,ps,pt,consistency)` (giữ `exec_cql` 3-arg làm wrapper → 0 churn call-site cũ); command `cql_exec` nhận `consistency`; `ipc.cqlExec` +arg; SqlWorkspace dropdown (cassandra only) → `tab.state.consistency` → `results.run(...,{consistency})`.
- **C5 — Create/Alter/Drop**: pure `sql/cassandra.ts` (`createTemplate`/`dropStatement`/`truncateStatement`) unit-tested; Create → CQL template vào editor; Drop/Truncate → confirm in-app (reuse `askConfirm`, backdrop không đóng) → `cqlExec` + refresh keyspace.
- **Gates**: check 0/0, vitest 482 (+`sql/cassandra.test.ts` 5), Rust lib 94 (+cassandra DDL 6 + `cql_change_sql` 1), tokens 0 mới (158<173). e2e: `cassandra-{cql,explorer,edit,ddl}.spec.ts` + `cassandra-workspace` xanh. Integration container `cassandra:5.0`: `cassandra_object_ddl_grid_edit_and_consistency` (C2 DDL round-trip + C3 INSERT/UPDATE/DELETE by PK verify SELECT + C4 ONE/QUORUM/LOCAL_QUORUM) EXIT=0.
- **CÒN LẠI (ngoài scope, giữ nguyên)**: SSL vẫn accept-any-cert (đã document); `connect_translating_to` (NAT/tunnel) chưa vào path prod; editable viewer v1 không sort server-side + filter chỉ partition key; Alter object = mở DDL sửa tay (chưa form).

## UI-FIX — Result panel hidden on fresh Query tab + Light-mode legible streaming colors (2 mục user)
1. **Result panel không hiện khi mở New Query, chỉ hiện khi Run/Explain**: trước đây `{#if !ui.resultPanelHidden}` render panel (kèm empty placeholder "Run a query…") cho MỌI tab kể cả tab mới chưa chạy gì. Giờ SqlWorkspace derived `resultVisible = !ui.resultPanelHidden && (exec || explain)` — tab mới (chưa Run) → editor fill toàn khung, KHÔNG panel; Run/Explain set exec/explain + `ui.showResultPanel()` → panel hiện. `exec`/`explain` là per-tab nên chuyển tab độc lập; Ctrl+J/X vẫn ẩn/hiện sau khi đã có kết quả. Gỡ empty-placeholder branch + `.wk-hide` CSS (không còn dùng). Cập nhật `result-toggle.spec.ts` (tab mới = no panel → Run → hiện → X ẩn → Ctrl+J hiện lại).
2. **Màu chữ NATS/Kafka/Redis khó nhìn ở Light mode** (ObjectExplorer + Result grids): các accent One-Dark-ish (cyan #56b6c2, amber #f0c674, yellow #e8c547, orange #e8923a, lavender #c4b5fd, green #27ae60…) wash-out trên nền sáng. Thêm palette **theme-aware `--sacc-{blue,cyan,green,amber,yellow,orange,purple,mauve,red}`** trong app.css: light = literal darkened (token-exempt, có justify comment như --grid-select/--sidebar); dark = `var(--hex-*)` gốc (pixel-diff baseline KHÔNG đổi). Swap usages: ObjectExplorer (Kafka topic→sacc-cyan, NATS stream→sacc-green, NATS subject→sacc-amber); RedisExplorer + RedisKeyView `TYPE_COLOR` map → sacc-*; RedisKeyView hash-field/label→sacc-yellow, stream id→sacc-orange; KafkaConsumer label→sacc-purple; NatsSubjectMessages subject→sacc-amber. JSON view popup đã dùng `--syntax-*` (theme-aware sẵn). Gates: check 0/0, vitest 519, tokens 190 (0 mới, = baseline), e2e result-toggle/pager/group-by/copy + redis-explorer/streaming-explorer/message-actions/explorer-databases/editor-autocomplete/proc-exec xanh.

## FIX — MySQL Query Editor mất suggest bảng/cột (defaultSchema không trỏ database đã chọn)
User báo: MySQL không còn suggest được (popup hiện keyword nhưng KHÔNG có tên bảng/cột), dù ĐÃ chọn database. **Root cause**: `SqlWorkspace.dbDefaultSchema` (feed `defaultSchema` cho lang-sql) lấy schema mặc định từ **cờ `is_default`** (`SCHEMA_NAME = DATABASE()`), KHÔNG dùng database user chọn. Khi cờ đó không trỏ đúng DB đã chọn (vd giải mã về false → fallback `schemas[0]` = database đầu bảng chữ cái), `defaultSchema` trỏ SAI database → lang-sql chỉ hoist bảng của schema mặc định thành gợi ý KHÔNG-qualify; bảng của DB đã chọn ĐÃ nạp nhưng chỉ hiện sau khi gõ `db.` → nhìn như "mất suggest". (MySQL/MariaDB/ClickHouse: schema == database.)
- **Fix frontend** (`SqlWorkspace.svelte`): `dbDefaultSchema` cho hệ schema-as-database (mysql/mariadb/clickhouse) → nếu `currentDb` (DB đã chọn) có trong danh sách schema thì trả THẲNG `currentDb` (bỏ qua is_default). PG/MSSQL giữ nguyên (is_default ?? schemas[0]); MSSQL đúng vì database ≠ schema. Bảng của currentDb đã được completion-effect nạp sẵn (nhánh `db`), giờ defaultSchema=currentDb → lang-sql suggest KHÔNG-qualify. No-op với demo (list_schemas demo chỉ trả `public`, current='library_db' không khớp → fallback cũ) nên e2e editor-autocomplete 6/6 xanh, không regress.
- **Fix backend** (`mysql.rs::schemas`, gốc của cờ): bọc so sánh trong `CAST(COALESCE(… , 0) AS SIGNED)` để `is_default` decode chắc chắn thành i64 (bare `=` result có thể trả type mà `try_get::<i64>` từ chối → None → is_default false âm thầm → sai default schema + sai đánh dấu "default" trên cây). `cargo build --lib` EXIT=0.
- Gates: check 0/0, e2e editor-autocomplete 6/6 + result-toggle xanh, rust lib build OK. **User cần `npm run tauri dev` (rebuild backend) để nhận cả 2 fix.**

## UI-FIX (tiếp) — Message grid Kafka/NATS: màu cột Time/Subject/Key/Value/Payload legible Light mode
Đợt trước chỉ sửa label header; các CỘT trong grid message vẫn dùng màu One-Dark washout trên nền sáng. Swap sang token theme-aware (dark KHỚP CHÍNH XÁC hex cũ → dark không đổi; light = biến thể đậm dễ đọc):
- **NatsSubjectMessages**: Time + Subject `--warn2`→`--sacc-amber` (dark=warn2 exact); Payload `--hex-98c379`→`--syntax-string` (dark #98c379 exact, light #50a14f).
- **KafkaConsumer**: Offset `--hex-d19a66`→`--syntax-number` (dark exact, light #b76b01); Time `--warn2`→`--sacc-amber`; Key `--hex-61afef`→`--syntax-function` (dark exact, light #4078f2); Value `--hex-5cc4e8`→`--syntax-operator` (light #0184bc; dark #56b6c2 — lệch nhẹ so #5cc4e8, cùng tông cyan, grid này KHÔNG thuộc pixel-diff baseline). Status banner `--warn2`→`--sacc-amber` + **fix typo `--danger`→`--error`** (token `--danger` không tồn tại → chữ error trước đây mất màu).
- Selected-row (cellColor trả `--hex-fff` khi chọn) không đổi; chỉ base non-selected dùng token mới. Gates: check 0/0, tokens 190 (0 mới), e2e message-actions 2/2 + streaming-explorer 9/9 xanh.

## UI-FIX — Kafka topic icon = đúng logo Kafka (đồng bộ SystemIcon)
User: icon cạnh topic name của Kafka sai, thay bằng đúng icon Kafka. Trước đó `KAFKA_LOGO` = `db-kafka.svg` (mark "cloud + nodes" màu cam #ff6a00 hardcode) — không giống logo Kafka + phớt lờ màu accent. Đổi `KAFKA_LOGO` (ObjectExplorer, CHỈ dùng cho topic row) thành inline SVG = **mark node-"K" y hệt SystemIcon** (icon Kafka canonical dùng cho connection), stroke `currentColor` → nhận `C.kafka` (var(--hex-8b5cf6) tím) đồng bộ với icon connection. `db-kafka.svg` giờ orphan (giữ lại, vô hại). Chỉ đụng 1 hằng số + không ảnh hưởng tính năng khác. Gates: check 0/0, tokens 190 (0 mới), e2e streaming-explorer 9/9 (Kafka topics list) xanh.

## UI-FIX — ObjectExplorer filter box rõ nét Light/Dark
User: ô filter trong ObjectExplorer (Kafka "Filter topics…", NATS, databases, folder, subjects, Redis pattern) mờ, khó thấy — phải rõ nét cả 2 theme. Nguyên nhân: input dùng `background:var(--panel)` + `border:var(--border)` — panel (#f4f6fb light) gần như trùng nền sidebar (`--sidebar` #e7ebf3) + border (#e2e6ef) quá nhạt → hộp chìm. Đổi TẤT CẢ filter input của Explorer sang `background:var(--raised)` (#fff light / #222838 dark — nổi trên nền sidebar cả 2 theme) + `border:var(--border2)` (#d2d8e4 / #333b4d — viền rõ). CHỈ đổi input filter (5 ô ObjectExplorer + Redis pattern), KHÔNG đụng nút (Add topic/stream vẫn --panel/--border). Gates: check 0/0, tokens 190 (0 mới), e2e folder-filter + streaming-explorer + redis-explorer 19/19 xanh.

## FEATURE — NATS stream row: icon Filter bên phải (mở subject filter nhanh)
User: thêm icon Filter bên phải StreamName trong NATS Explorer để lọc subjects dễ hơn (trước chỉ mở qua context menu "Filter subjects…"). Thêm props tái dùng cho `row` snippet: `onFilter?: ()=>void` + `filterActive?: boolean` (TÁCH BIỆT với `filterable` vốn drive folder-filter chung — không đụng). Row render funnel (FILTER_ICON) bên phải khi có `onFilter`, `stopPropagation` (không toggle expand), highlight `--primary` khi active. NATS stream row truyền `onFilter: toggle openSubjFilter/clearSubjFilter theo subjFilterOpen`, `filterActive: subjFilterOpen.has(name)`. Click funnel → expand stream + hiện ô "Filter subjects…"; click lại (đang active, title "Clear filter") → đóng. Context menu "Filter subjects…"/"Clear filter" cũ giữ nguyên. e2e streaming-explorer +1 (`NATS stream funnel icon opens/closes the subject filter`: click funnel→ô hiện→gõ "eu" lọc→click lại đóng). Gates: check 0/0, tokens 190 (0 mới), e2e streaming-explorer 10/10.

## FIX — NATS funnel: focus ngay vào ô Filter khi nhấn icon
User: nhấn icon Filter phải focus ngay vào ô Filter. Ô subject filter trước dùng `autofocus` — KHÔNG đáng tin khi mở động từ funnel (funnel span vừa click đang giữ focus, autofocus chỉ fire lúc render đầu). Đổi sang `use:focusFilter` (action `node.focus()` on mount — đã dùng cho folder filter) → focus chuyển thẳng vào ô ngay khi hiện. e2e streaming-explorer funnel test +assert `toBeFocused()`. Gates: check 0/0, tokens 190 (0 mới), e2e streaming-explorer 10/10.

## FEATURE — ResultGrid tô màu cột kiểu số (relational only)
User: cột kiểu decimal/double/float/int/smallint… (number) trong ResultGrid tô màu; CHỈ database relational. Impl: ResultGrid thêm prop `system?`; `numericCols` = cột có `classifyType(type) ∈ {int,bigint,float,decimal}` (bao smallint/integer/bigint, numeric/decimal/money, real/double/float), CHỈ khi `system ∈ {postgres,mysql,mariadb,mssql,sqlite}` (`colorNumbers`; ClickHouse/Cassandra/Mongo loại — đúng "relational only"). Value span nhận `color:var(--syntax-number)` (theme-aware, đã dùng ở Objects tab; light #b76b01 / dark #d19a66) — **nhường chữ trắng khi cell/row selected** (`!isCellSelected && !isRowSelected`). Plumb `system`: SqlWorkspace→ResultPanel→ResultGrid; các viewer khác (TableViewer/Cassandra/Mongo) fallback `editTarget.system` (Cassandra/Mongo không relational → không tô). Chỉ đổi span hiển thị; NULL/''/edit input/JSON badge giữ nguyên; canh lề không đổi (số vẫn tabular-nums). e2e `result-number-color.spec.ts` (gpa numeric = rgb(209,154,102) khác first_name text). Gates: check 0/0, tokens 190 (0 mới), e2e result-number-color 1/1 + result-pager/group-by/result-copy/result-toggle/cassandra-edit 5/5.

## FEATURE (tiếp) — ResultGrid cột số canh phải (DataGrip-style)
User: "numeric phải nằm ở bên phải". Cùng scope relational + `numericCols` (đã gate). Áp cho cột số: header `th` `text-align:right`; cell display div `justify-content:flex-end`; inline edit input `text-align:right` (số canh phải cả khi sửa). Cột text giữ canh trái. Không đụng NULL/'' badge (nằm trong div đã flex-end nên NULL cũng dồn phải — hợp lý cho cột số). e2e result-number-color +assert `justifyContent==='flex-end'` cho 3.9 (numeric), khác cho 'An' (text). Gates: check 0/0, tokens 190 (0 mới), e2e result-number-color/result-pager/group-by/result-copy 4/4.

## FIX — ResultGrid context menu bị cắt (clamp theo chiều cao thật)
User: context menu (Copy cell/row/column/Paste + 7 format Copy-as) bị che khuất, không thấy hết (XML mất). Root cause: vị trí menu clamp bằng offset CỨNG `top:min(m.y, innerHeight - 330)` — menu đầy đủ (~11 item) cao HƠN 330 → vẫn tràn đáy. Fix: action `placeMenu(node,{x,y})` đo `getBoundingClientRect()` THẬT sau mount → clamp x/y để menu nằm trọn trong viewport (margin 8px), cập nhật `left/top` trực tiếp; thêm `max-height:calc(100vh - var(--px-16));overflow-y:auto` (fallback khi viewport thấp hơn cả menu). Bỏ offset cứng 330/220. e2e result-copy +1 (`stays fully inside the viewport`: right-click hàng CUỐI (grid ở panel dưới → menu mở thấp) → assert `box.y+box.height <= innerHeight` + XML visible). Gates: check 0/0, tokens 190 (0 mới), e2e result-copy 2/2 + result-number-color/group-by xanh.

## 3 mục người dùng — Export XML + Properties (guide+fix) + Compare hover/selected
1. **Export XML (ResultGrid)** ✅: thêm format **XML** vào (a) quick Export ▾ (ResultPanel: `FMT_META.xml`, `serialize` xml → `toXml` reuse của "Copy as XML", `doExport` union, menu item) + (b) Export wizard (ExportDialog: Fmt/EXT/MIME + `serialize` xml + radio). Streaming gate đổi `format!=='xls'` → `['csv','json','sql'].includes(format)` (xml/xls buffer, đúng vì exportQueryToFile chỉ csv/json/sql). `toXml` đã unit-test sẵn. e2e export-wizard +1 (Export ▾ → XML visible → download `.xml`). KHÔNG đụng format cũ.
2. **Properties panel — ĐÃ implement** (properties-panel.spec xanh): ObjectExplorer publish `propTarget` (từ `treeSel`) → `properties.set` → PropertiesPanel render columns/indexes/definition. **Cách hiện**: panel ẩn mặc định → mở bằng tay cầm dọc "⇤ Properties" (title "Show Properties panel") ở mép phải → **single-click** 1 table/view/column/proc trong Explorer. **Fix 2 gap**: (a) sequence key `sq:` chưa map (kindMap chỉ có `seq`) → thêm `sq`; (b) bảng ở **database khác** (foreign-db subtree PG/MSSQL, key `fdb:<db>:s:<schema>:<fk>:<name>[:col:…]`) trước KHÔNG publish (prefix `fdb` không parse) → thêm parse foreign-db dùng `dbSubId[db]` làm connId (PropertiesPanel load detail trên sub-connection đúng DB).
3. **Compare hover/selected** ✅ (SchemaCompare, dùng cho cả Compare databases & Compare schemas): thêm `<style>` `.cmp-row.clickable:not(.sel):hover{background:var(--hover)}` + `.cmp-row.sel{background:color-mix(in srgb,var(--primary) 22%,transparent)}` + `.cmp-panel{background:var(--panel)}`; áp `class="cmp-row [cmp-panel]" class:sel={selDiff===d} class:clickable` cho row table-level + nestedObj. Selected = row đang xem DDL diff. Màu dùng token sẵn có, base bg giữ nguyên (không đổi giao diện cũ). e2e schema-compare +assert `.cmp-row.sel` count=1 sau click.
- Gates: check 0/0, tokens 190 (0 mới), e2e export-wizard 3/3 + schema-compare 3/3 + properties-panel 2/2 + result-copy 2/2.

## FIX — Compare hover/selected KHÔNG thấy (màu quá nhạt + selected bị modal che)
User báo hover/selected vẫn không thấy. 2 nguyên nhân: (a) hover dùng `var(--hover)` (#eef1f7) ~ trùng `--panel`/surface ở light → chìm; (b) "selected" = `selDiff===d` nhưng click row có DDL MỞ modal che row + đóng modal xóa selDiff → không bao giờ thấy row selected. Fix:
- **Selected bền vững**: thêm state `selRow` (key row click gần nhất), độc lập với modal DDL. Mọi row click set `selRow` (table-level/nested/column) → `.cmp-row.sel` = `selRow===key`; đóng modal KHÔNG xóa → row vẫn sáng.
- **Màu rõ (dùng đúng convention Explorer tree)**: hover `.cmp-row:not(.sel):hover` = `color-mix(--primary 9%)` (thay --hover); selected `.cmp-row.sel` = `var(--rgba-91-124-255-_16)` (16% xanh, đã dùng cho tree.selected); sel:hover = 22% primary. BỎ accent bar (row có sẵn status color bar bên trái → chồng lấn).
- Áp `.cmp-row` cho CẢ column rows (trước chỉ display) → giờ hover+select được; base bg giữ qua `.cmp-panel`.
- e2e schema-compare mở rộng: sau click row → `.cmp-row.sel` count=1 + backgroundColor ≠ transparent; **đóng modal → vẫn count=1** (selected persist). Behavior cũ (mở DDL/expand/checkbox) giữ nguyên.
- Gates: check 0/0, tokens 190 (0 mới), e2e schema-compare 3/3.

## MONGODB M6 — 5 mục parity UX (Design Document · pagination · autocomplete · number color · New Database)
Chỉ MongoDB, additive, frontend-orchestrated qua `mongo_exec` (driver đã có updateMany/count/createCollection — KHÔNG thêm backend command).
1. **Design Document** (ctx menu collection): pure `mongo/design.ts` (`buildFieldOps` → updateMany $set add-if-missing / $rename / $unset; order add→rename→drop) + `DesignDocumentDialog` + `designDocWizard`. Nạp fields (list_columns), rename/drop (chặn `_id`)/add (default JSON), preview, Apply tuần tự + refresh.
2. **Open Document pagination**: `MongoCollectionView` từ "Load next page" → page-based (page/pageSize 100-1000 + `countDocuments` total + « ‹ › » footer) đồng bộ Table Viewer quan hệ; find.skip.limit.
3. **Autocomplete Mongo editor**: pure `mongo/complete.ts` (`parseMongoCollection`/`isCollectionPrefix`) + `SqlWorkspace.mongoCompletionSource` (dùng thay columnSource khi systemType==='mongodb'): `db.`→collections, trong query→fields của collection tham chiếu; lazy từ explorer.cache theo currentDb.
4. **Number color grid**: `ResultGrid.NUM_COLOR_SYSTEMS` +`'mongodb'`; MongoCollectionView truyền `system="mongodb"`; `classifyType` +`long`→bigint → int/long/double/decimal tô `--syntax-number`.
5. **New Database (Mongo)**: ctx menu hiện cho mongodb; `NewDatabaseDialog` nhánh mongo +field "First collection" → `mongoExec(db.createCollection(coll), dbName)`.
- Tests: unit design(9)+complete(5)+types(+long/double/decimal); e2e mongo-explorer 9/9 (Design/NewDB/autocomplete/pager+color); integration `mongo_new_database_design_document_and_pagination` (mongo:7) EXIT=0. Gates: check 0/0, vitest 531, tokens 190 (0 mới). Spec: SPEC-MONGODB-FEATURE.md §M6.

## MONGODB M6.1 — Tree expand chỉ qua double-click
User: MongoExplorer single-click KHÔNG expand, chỉ double-click mới mở. Đổi binding: database + collection node → onclick chỉ select, ondblclick toggle (expand/collapse); chevron ▸/▾ single-click vẫn toggle (tiện). **Collection double-click giờ = expand fields/indexes** (trước = mở documents); Open Documents chuyển hẳn về context menu (không mất tính năng). Auto-expand default db lúc connect giữ nguyên (gọi toggleDb trực tiếp). Chỉ đụng Mongo. e2e mongo-explorer +1 (single-click không collapse → dblclick collapse/expand → dblclick collection hiện first_name, không mở tab viewer) → 10/10. check 0/0. Spec: SPEC-MONGODB-FEATURE.md §M6.1.

## MONGODB M6.2 — Query Editor mongosh + double-click documents + shared grid
1. **Query Editor**: user gõ SQL không chạy (editor là mongosh, KHÔNG phải SQL — run path `results.run→mongoExec` đã đúng, F5+Ctrl+Enter đều chạy). Fix UX: bỏ SQL lint cho mongodb (`lintDoc` early-return) → hết squiggle sai. Autocomplete collections/fields đã có (M6). Cách query: `db.<coll>.find({filter},{proj}).skip().limit().sort()`, aggregate, countDocuments, insert/update/deleteOne (update/delete phải có filter).
2. **Double-click collection = Open Documents** (khôi phục sau M6.1): double-click collection→openMongoCollection; single-click=select; chevron single-click=expand fields/indexes; double-click DB=expand collections. Không mất tính năng.
3. **Shared component**: MongoCollectionView ĐÃ dùng chung `ResultGrid` + `EditTarget`→apply_grid(by _id) + exportWizard + footer pager kiểu Table Viewer. Shell riêng vì mongosh/JSON filter khác SQL (không hợp nhất TableViewerTab để tránh rủi ro relational).
- Tests: e2e mongo-explorer 10/10 (Run F5 + dbl-click documents + dbl-click DB expand); integration `mongo_query_editor_find_filter_projection_limit` (filter/projection/limit) EXIT=0. check 0/0. Spec §M6.2.

## MONGODB M6.3 — Ctrl+N mở đúng Mongo console + DB dropdown cho Mongo
1. **Ctrl/Cmd+N**: MongoExplorer chưa publish `explorer.selectedDatabase` → Ctrl+N mở "Untitled query" không bind db. Fix: MongoExplorer `$effect` set `selectedDatabase={base:connId, database:<db từ selectedKey>}`; ObjectExplorer guard dbTarget effect `if(system!=='mongodb')` (không ghi đè, không đụng relational); `openQueryConsole` title='Untitled Mongo' cho mongodb. → chọn db `analytics` → Ctrl+N → tab "Untitled Mongo" bound analytics.
2. **DB dropdown Mongo**: SqlWorkspace `supportsMongoDb` + `showDbPicker`; dropdown (list_databases) cho Mongo, pick→tab.state.database→currentDb→autocomplete+runOpts.database vào mongoExec (KHÔNG attach sub-conn, resolveRunConn giữ nguyên).
- Tests: e2e mongo +2 (Ctrl+N bound analytics + dropdown switch); relational Ctrl+N/dropdown 15/15 regression xanh; check 0/0, vitest 531, tokens 190. Spec §M6.3.

## MONGODB M6.4 — Query Editor suggest hàm MongoDB (methods + operators)
User: editor Mongo chỉ suggest collection, thiếu hàm. Thêm vào `mongoCompletionSource`: pure `mongo/functions.ts` MONGO_METHODS (sau `db.<coll>.`) + MONGO_OPERATORS (gõ `$`: comparison/logical/element/array/update/aggregation) — vocab tĩnh check TRƯỚC guard connection (suggest ngay). Detect `isMethodContext`/`isOperatorContext` (complete.ts). Thứ tự method→operator→collection→field. `SqlEditor.langExt` bỏ `fnSource` (SQL functions) cho mongodb (relational giữ nguyên). Tests: unit functions.test(4)+complete.test(+6) → mongo unit 19; e2e mongo +1 (db.students.→find/aggregate, $g→$gt); integration `mongo_suggested_methods_and_operators_execute` (find $gt/$in/$or·updateMany $set+$inc·aggregate $match/$group/$sum·distinct·deleteMany) EXIT=0. check 0/0, tokens 190, editor-autocomplete 6/6. Spec §M6.4.

## FUNCTIONS — Query Editor suggest ĐẦY ĐỦ hàm mỗi relational (hybrid introspection + static)
Review trước phát hiện autocomplete chỉ có ~11 hàm curated/engine + keyword list lang-sql (thiếu nhiều: PG `to_char/date_trunc/regexp_replace/split_part/jsonb_agg`…; MySQL/MSSQL/SQLite `builtin` của lang-sql là CLI-meta không phải hàm). User chốt hướng **hybrid introspection cả backend + frontend**, chỉ suggest (KHÔNG tô màu).
- **T-F1 Backend `list_functions(conn_id, schema)`** → `FunctionInfo{name, signature?, detail?}` (types.rs). Introspect nơi server liệt kê được: **PG** `pg_proc` (pg_catalog built-ins + schema user/extension; lọc operator-support qua `NOT EXISTS pg_operator` + bỏ internal/cstring; fold overload; kèm `pg_get_function_arguments` → signature), **SQLite** `pragma_function_list()` (đủ built-in + extension đã load), **ClickHouse** `system.functions`. **MySQL/MSSQL** built-in KHÔNG enumerable → chỉ trả **user functions** (information_schema.ROUTINES type=FUNCTION / sys.objects FN·IF·TF·AF·FS·FT); built-in do frontend bù static. Non-relational → rỗng. Command + register lib.rs + `ipc.listFunctions` + demo mock. Dispatch `LiveConnection::functions` (mod.rs).
- **T-F2 Static catalog** `src/lib/sql/functions.catalog.ts` (`staticFunctions(system)`): MySQL ~250, MariaDB = MySQL + extras (to_char/json_query…), MSSQL ~200 built-in phân theo category (string/numeric/datetime/aggregate/window/json/control/conversion/system) → `FnSig{name, signature:'name(…)', detail:category}`. PG/SQLite/CH = [] (introspection lo).
- **T-F3 Merge** `functionCatalog(system, dynamic)` (functions.ts): gộp static built-ins + dynamic (introspected) + curated signatures, dedup case-insensitive theo tên, **ưu tiên chữ ký thật** (`name(args)` thắng placeholder `name(…)`, curated thắng tất cả), sort. `SqlEditor` prop `dynamicFunctions?: FnHint[]` → `fnSource` dùng `functionCatalog`; `$effect` reconfigure khi list tới. `SqlWorkspace`: `$effect` lazy `ipc.listFunctions(acConnId, dbDefaultSchema)` cache theo `conn::schema`, guard stale, chỉ relational (Mongo giữ nguồn mongosh riêng — SqlEditor bỏ fnSource cho mongodb).
- **Tests**: unit `functions.test.ts` 12 (static comprehensive/unique/detail; PG-SQLite-CH rỗng static; merge dedup + curated-thắng-placeholder + dynamic-upgrade + sorted); e2e `editor-autocomplete` +2 (PG `date_tr`→`date_trunc` từ server; MySQL `json_ex`→`json_extract` từ static) → 8/8; integration `pg_full_introspection…` (built-ins to_char/date_trunc/regexp_replace/split_part/jsonb_agg + user emp_count + tên unique + signature) + `sqlite_introspection` (pragma_function_list ≥20 + json_extract/substr/strftime…) + `mysql_explorer_tree_introspection` (user f_double) EXIT=0.
- **CÒN LẠI**: MySQL/MariaDB/MSSQL phụ thuộc static built-in (maintain theo version); PG/SQLite/CH tự đầy đủ qua introspection. Chưa tô màu hàm (user chọn skip). Cassandra CQL editor không dùng path này.
- Gates: check 0/0, vitest 547, e2e editor-autocomplete 8/8 + mongo-explorer 13/13, integration PG/SQLite/MySQL functions EXIT=0, tokens 143 (0 mới, < 190 baseline).

## FUNCTIONS (tiếp) — tô màu hàm trong editor + khử trùng gợi ý (DataGrip-style)
User chốt: bật tô màu hàm + khử trùng (giống DataGrip/Navicat — bundle static + 1 danh sách + tô màu hàm). Chỉ suggest+màu, không đổi hành vi chạy.
- **BẪY 1 — KHÔNG nhét function vào `builtin` dialect để tô màu**: builtin làm tokenizer đọc tên hàm thành keyword hoàn chỉnh → khi table prefix trùng tên hàm, completion vỡ (gõ `ord` = hàm MySQL `ORD` → nuốt bảng `order`; debug: `o/or/orde/order` ra bảng, riêng `ord` ra `[]`). ⇒ tokenizer giữ nguyên dialect gốc.
- **BẪY 2 — KHÔNG hand-wire `new LanguageSupport(base.language, [schemaCompletionSource, …])`**: schema/alias/reserved-quoting của lang-sql tinh vi, tự dựng làm **vỡ alias-column completion** (`s.or`→`s."order"` không hiện fresh; reserved-quote.spec fail). ⇒ **DÙNG `sql({dialect, schema, defaultSchema})` nguyên bản** (giữ y hệt hành vi cũ) rồi CHỒNG thêm decoration + fnSource.
- **Tô màu = ViewPlugin decoration** (`functionHighlighter`): regex `\b(\w+)\s*(?=\()` trên visibleRanges → class `.cm-sql-fn` (`var(--syntax-function)`, theme-aware) CHỈ khi tên ∈ `fnColorSet(sys)` = `functionCatalog(sys, dynamicFunctions)` ∪ dialect builtin (CH_FUNCTIONS). **KHÔNG trừ keyword** (bug đầu: trừ keyword làm mất màu hàm-cũng-là-keyword như MSSQL `GETDATE`/`DATEADD`); `IN (`/`VALUES (` không tô vì `in`/`values`/`exists` không nằm trong catalog. Gồm cả hàm introspect (PG `date_trunc`) vì rebuild theo `dynamicFunctions`. Không đụng tokenizer.
- **Khử trùng**: hàm từ static/introspected catalog (json_extract, date_format, getdate…) **KHÔNG phải keyword dialect** → chỉ hiện 1 lần (từ fnSource kèm chữ ký). Chỉ ~11 hàm-cũng-là-keyword (count/coalesce/…) hiện đôi — **y hệt app gốc từ trước** (không phát sinh trùng mới). fnSource guard: sau `.` (chỉ cột) + table-position (sau FROM/JOIN/INTO/UPDATE/TABLE → không lấn tên bảng).
- **langExt relational** = `[sql({dialect: baseDialect(sys), schema, defaultSchema}), functionHighlighter(sys), fnSource(sys), columnSource]`. MongoDB giữ path `sql()` cũ (mongosh).
- **Tests**: e2e `editor-autocomplete` +3 (catalog fn `json_extract` 1 dòng có chữ ký = dedup; `date_trunc(x)` span `.cm-sql-fn` = màu; **MSSQL `SELECT DATEADD(day,1,GETDATE())` → cả 2 tô màu** = fix keyword-fn) → 11/11; `reserved-quote` 2/2 (alias-column `s."order"` không regress); reserved-quoting MySQL/PG + columns + table suggest vẫn xanh.
- Gates: check 0/0, vitest 547, e2e editor-autocomplete 11/11 + reserved-quote 2/2, tokens 143 (0 mới; thêm `.cm-sql-fn` dùng var), integration list_functions PG/SQLite/MySQL/MSSQL/ClickHouse EXIT=0.

## FIX — Theme Light/Dark lưu qua restart (localStorage, no-flash)
User: chọn Light/Dark phải giữ khi tắt/mở lại app. Trước đó CHỈ lưu qua `app_state` backend (desktop persist đúng nhưng **có flash**: main.ts luôn add `.dark` lúc boot rồi `loadPersisted` async mới đổi; nếu async chậm/lỗi thì kẹt dark) và **web/demo KHÔNG lưu** (demo `set_app_state` = no-op).
- **main.ts**: thay `classList.add('dark')` cứng → `classList.toggle('dark', localStorage.getItem('theme') !== 'light')` — đọc localStorage **đồng bộ TRƯỚC first paint** → đúng theme ngay, không flash, chạy cả WebView2 lẫn browser (localStorage sống qua restart cả 2).
- **ui.svelte.ts**: `toggleTheme`→`setTheme(theme)` ghi **cả localStorage LẪN app_state**; `loadPersisted` đọc localStorage trước (khớp cái boot đã áp), fallback `app_state` cho install cũ + mirror ngược vào localStorage (migrate → boot sau flash-free). try/catch cho private-mode.
- e2e `theme-persist.spec.ts`: toggle Light → reload (=restart) → vẫn Light + label "Light"; toggle Dark → reload → Dark. Persist qua localStorage nên chạy được trên demo (set_app_state no-op không cản). Gates: check 0/0, vitest 547, e2e theme-persist 1/1.

## FIX — Query Editor mất suggest khi tên DATABASE chứa dấu chấm (vd `crm.ismart.edu.vn`)
User (MySQL native trên Debian, screenshot có DB `bi.ismart.edu.vn`/`bo.review`/`crm.ismart.edu.vn`): cây Explorer hiện đủ bảng, đã chọn database nhưng Query Editor KHÔNG suggest bảng/cột. **Root cause (xác nhận trong `@codemirror/lang-sql` dist/index.js)**: với MySQL/MariaDB/ClickHouse "database == schema" nên tên DB được đưa làm KEY namespace + `defaultSchema` cho lang-sql. lang-sql coi `.` là dấu phân tách định danh: `addNamespaceObject` (index.js:510) tách key trên dấu chấm chưa-escape → 88 bảng của `crm.ismart.edu.vn` bị nhét vào path lồng giả `crm › ismart › edu › vn`; đồng thời `defaultSchema = top.child(name)` (index.js:541, `child()` KHÔNG tách/không unescape) trỏ vào 1 level rỗng keyed nguyên chuỗi → **mismatch → gõ bảng trần không có gợi ý nào**. Cây Explorer không đi qua lang-sql nên vẫn hiện; chọn DB chỉ load bảng chứ không cứu được lang-sql; demo dùng `public` (không dấu chấm) nên test cũ không bắt được.
- **Fix (bất đối xứng, quan trọng)**: pure `src/lib/sql/completion-schema.ts::escapeSchemaKey(name)` = `name.replace(/\./g, '\\.')`. Trong `SqlWorkspace.completionSchema` **CHỈ escape KEY** (`ns[escapeSchemaKey(schemaName)] = tables`); `detail`/`defaultSchema` giữ **RAW** (real dots). Lý do: lang-sql tách key escaped rồi UNESCAPE `\.`→`.` khi tạo level (index.js:519) → level keyed = tên thật; còn `defaultSchema` raw qua `child()` (không unescape) phải khớp tên thật đó → 2 bên **hội tụ cùng 1 CompletionLevel**, bảng nằm trong defaultSchema → suggest trần chạy. `columnSource`/`schemaOfTable`/`dbDefaultSchema` giữ nguyên key gốc (không qua lang-sql).
- **Tests** `completion-schema.test.ts` (7): escape đúng chuỗi + tái hiện split của lang-sql (raw→4 segment, escaped→1) + hội tụ với defaultSchema raw + **tích hợp thư viện THẬT** (`schemaCompletionSource({dialect:MySQL, schema, defaultSchema})` gõ `select * from tm`): escaped key → gợi ý `tm_biz_collect`/`tm_activities`; raw key → KHÔNG (control tái hiện bug). Gates: check 0/0, vitest 554, e2e editor-autocomplete + reserved-quote 13/13 (không regress DB tên thường như `public`).

## FIX — MySQL cột `_bin` collation hiện `0x…` hex thay vì text (vd `receipt_no varchar(100) COLLATE utf8_bin`)
User (MySQL native): query cột `receipt_no` ra `0x50542d64…` (thực ra là ASCII "PT-d…"), nhưng Design/tree hiện đúng `varchar(100)`. **Root cause (xác nhận probe + đọc sqlx 0.8.6)**: cột là `varchar` charset `utf8` NHƯNG collation **`utf8_bin`** (một collation `_bin`). MySQL đặt cờ protocol **`BINARY_FLAG` (128)** lên metadata của MỌI cột `_bin` (ý nghĩa cờ = "binary collation", không chỉ charset `binary`). sqlx quyết định type CHỈ theo cờ đó (`column.rs:170` `is_binary = flags.contains(BINARY)` → `VarChar/VarString if is_binary => "VARBINARY"`), charset `utf8` cũng mặc kệ → header cột = `varbinary`. Rồi `decode_value` (mysql.rs) hex-hoá vô điều kiện họ VARBINARY/BLOB → text UTF-8 ra `0x…`. Catalog `COLUMN_TYPE` không phụ thuộc collation → báo đúng `varchar(100)` (nên Designer đúng). **Theo TỪNG cột** (chỉ cột `_bin`), không phải toàn connection, không phải results-charset (sqlx mặc định `SET NAMES utf8mb4`). Container test cũ dùng collation `_ci` mặc định → không dính → che bug. App khác (mysql CLI/DBeaver) hiện đúng vì decode bytes theo charset bất kể cờ; driver kiểu-tĩnh như sqlx tin vào cờ (lỗi sqlx đã biết với cột `_bin`).
- **Fix data** (`drivers/mysql.rs`): tách `BIT` (giữ hex thuần qua `to_hex`) khỏi họ string-carrying `VARBINARY/BINARY/*BLOB` → dùng `bytes_to_value`/`text_from_binary`: **thử UTF-8 trước** (giống helper `text()` của introspection), hợp lệ **và không chứa NUL** → trả String; ngược lại `0x…` hex. Cột `_bin` (text) hiện đúng; BLOB/binary thật (không phải UTF-8 hợp lệ hoặc có NUL) vẫn hex → không làm hỏng ảnh/bytes. KHÔNG đụng connection/charset.
- **Fix header** (cùng file): gộp giải mã vào `decode_rows(rows) -> (cols, out_rows)` (dùng chung cho `exec` + `exec_params`) — sau khi decode, **relabel type của cột họ-binary mà MỌI giá trị non-null đều là text** (không ô nào phải hex) sang type text tương ứng (`text_type_for`: varbinary→varchar, binary→char, blob→text, tinyblob→tinytext, …). Phán định text-vs-binary trên **bytes THÔ** (`text_from_binary`, không đoán từ chuỗi "0x…" đã render → không mơ hồ). Cột còn ≥1 giá trị binary thật, hoặc toàn NULL → giữ nguyên `varbinary` (an toàn). BIT không thuộc `is_binary_family` nên không bao giờ bị relabel. → header hiện `varchar` thay vì `varbinary`; đồng thời edit grid dùng literal chuỗi (đúng cho cột text) thay vì coi là binary.
- **Tests**: Rust unit `drivers::mysql::tests` (5: `to_hex`; `_bin` text + UTF-8 đa byte "Nguyễn" + rỗng → text; binary invalid-UTF8 + có-NUL → hex; `text_from_binary` phân biệt; `is_binary_family`/`text_type_for` mapping) — EXIT=0. Integration `mysql_bin_collation_text_column_is_not_hex` (tạo `varchar COLLATE utf8mb4_bin` + `varbinary`, insert text + UNHEX('00ff01fe') → SELECT: receipt_no ra text + **cols type = `varchar`**, blob_col ra `0x00ff01fe` + **cols type = `varbinary`**) — **compile OK (--no-run EXIT=0), chưa chạy vì Docker Desktop chưa bật** (chạy lại khi có Docker). Gates: check 0/0 (frontend không đổi), cargo build --lib + test --no-run EXIT=0, unit 5/5.

## USERS & PRIVILEGES — SPEC-USERS-PRIVILEGES.md (nhánh `feat/users-privileges`)
Spec đầy đủ 8 engine (PG/MySQL/MariaDB/MSSQL/ClickHouse/Cassandra/MongoDB/Oracle; Redis/Kafka/NATS ngoài phạm vi; SQLite N/A ẩn entry). Quyết định chốt §17: entry point đổi thẳng, ClickHouse row-policy/quota v1 read-only, PG default-priv v1 chỉ checkbox future-tables, MSSQL tạo SQL+Windows login (AAD chỉ hiển thị). Ràng buộc cứng: §1.0 cách ly additive-only; §1.4b đường thực thi per engine (MySQL BẮT BUỘC text-protocol qua exec_statement, cấm exec_params → tránh lỗi 1295; MSSQL raw batch; Cassandra cql_exec; Mongo command); §1.9 Definition-of-Done 6 bước CREATE→LOGIN→DENIED→GRANT→WRITE-DENIED→REVOKE trên container thật.
- **U0 — Khung nền — ✅ DONE**. Tab type `'user-manager'` (types.ts) + `tabs.openUserManager(connId, focus?)` singleton (tabs.svelte.ts) + nhánh App.svelte paneBody + `UserManagerView.svelte` (shell: list principal qua `ipc.usersView`, chọn → detail, Refresh rule chung; per-engine detail/dialog/grid làm ở từng phase). Backend `commands/users_admin.rs`: pure `quote_ident`/`quote_str`/`mysql_account` (8 dialect, escape `'"`]\`) + `users_query` skeleton (PG roles/users view) + command `users_view` (register lib.rs) + `ipc.usersView` + demo case `users_view` (fixture roles/users). Entry point: nút "Users & privileges" toolbar Explorer đổi từ `openAdminView(_,'users')` → `openUserManager` (§17.1). Gates: check 0/0, vitest 596, playwright user-manager 1/1 + regression (explorer-databases/audit5-ui/connection-toolbar) xanh, Rust lib unit 4/4 (users_admin), tokens 0 mới. **STOP chờ user duyệt trước khi sang U1 (PostgreSQL).**
- **U1 — PostgreSQL — ✅ DONE**. Pure builder `src/lib/users/postgres.ts` (createRole/alterRoleOptions/alterPassword/rename/drop/dropRoleOwned; grantMembership/revoke; grantOnTable; **§1.8.3 schema presets** presetReadOnly/ReadWrite/ReadWriteExecute/Full/RevokeAll + futureTables→ALTER DEFAULT PRIVILEGES cho schema owner; grantConnect; schemaPreset dispatch) unit-test 12. Backend `users_admin.rs` PG views: roles/members/db_grants/schema_grants/table_grants/default_acl/schema_owners/can_manage (Rust unit 5). Frontend: `PgUserManager.svelte` (list login/group role + SUPER badge; tabs General [attributes + change password queue + drop confirm] · Membership [grant/revoke] · Privileges [grid schema×SELECT/INSERT/UPDATE/DELETE, cell ✓/■/☐ từ table_grants aggregate, preset R/RW/RW+X/Full/Revoke + futureTables checkbox]; Pending changes preview highlightSql + Execute tuần tự → reload introspection) + `PgCreateRoleDialog.svelte` popup (pgAdmin checkbox attributes, password Show/Generate, live CREATE ROLE preview mask password, nút "Create login/group role" theo Can login?) + store `pgrole.svelte.ts` + mount App.svelte + dispatch UserManagerView. Demo: users_view views (members/table_grants/schema_owners/can_manage). Tests: unit 12 (postgres.ts) + Rust 5 (users_admin); e2e `user-manager.spec.ts` 3 (list+attributes / New Role popup không mở tab + backdrop không đóng / preset queue GRANT); **integration `pg_user_manager_end_to_end` EXIT=0 trên container thật — 6 bước §1.9: CREATE→LOGIN(connect user mới)→SELECT denied→GRANT read-only→SELECT OK→INSERT denied→REVOKE→denied lại→DROP sạch**. Gates: check 0/0, vitest 608, playwright user-manager 3/3 + regression xanh, Rust lib unit + integration EXIT=0, tokens 0 mới. **STOP chờ user duyệt trước khi sang U2 (MySQL/MariaDB).**
- **U2 — MySQL + MariaDB — ✅ DONE** (1 component adapt theo systemType, DRY — spec liệt kê 2 file riêng, ghi deviation). Pure builder `src/lib/users/mysql.ts` (acct 'u'@'h' escape '/\; createUser [plugin WITH/VIA unix_socket, password expire, lock]; alterPassword/lock/expire/rename/dropUser; grant per level global/schema/table + grantColumns + revoke + revokeAll; **§1.8.2 dbPreset** read-only/read-write/+execute/full/revoke-all = 1 statement/scope; createRole/dropRole/grantRole/revokeRole; **setDefaultRole MySQL `TO` vs MariaDB `FOR` (no ALL)**) unit 6. Backend `users_admin.rs` MySQL/MariaDB views: users (mysql no is_role / mariadb is_role), schema_privs/table_privs/global_privs, role_edges (mysql) / roles_mapping (mariadb), default_roles, grants_for (SHOW GRANTS via arg) — Rust unit +1 (7 total). Frontend: `MySqlUserManager.svelte` (account list user@host + LOCK/role badge; tabs General [plugin/password/lock/rename/drop] · Schema Privileges [grid Global+per-db × SELECT/INSERT/UPDATE/DELETE/EXECUTE, cell từ schema_privs(✓)/table_privs(■)/global_privs, preset R/RW/RW+X/Full/Revoke] · Roles [grant/revoke + default role]; Pending changes + Execute) + `MySqlCreateUserDialog.svelte` popup (User Name + **Host bắt buộc** datalist %/localhost/IP + plugin per-engine + password Show/Generate + live CREATE USER preview) + store `myuser.svelte.ts` + mount App.svelte + dispatch UserManagerView (mysql|mariadb). **§1.4b tuân thủ: execute qua ipc.execStatement (TEXT protocol) — không exec_params → tránh lỗi 1295.** Demo: users_view MySQL fixtures. Tests: unit 6 (mysql.ts) + Rust 7 (users_admin); e2e `user-manager.spec.ts` +1 (MySQL account list + preset backtick GRANT + Add Account popup không mở tab) = 4 total; **integration `mysql_user_manager_end_to_end` + `mariadb_user_manager_end_to_end` EXIT=0 container thật — 6 bước §1.9** (CREATE default plugin → LOGIN [no default db, qualified names] → denied → GRANT db read-only → OK → INSERT denied → REVOKE → denied → DROP; MariaDB thêm: is_role='Y' verify + CREATE ROLE + GRANT role + SET DEFAULT ROLE … FOR → user đọc qua default role). Gates: check 0/0, vitest 614, playwright user-manager 4/4 + audit-hotkeys 13/13, Rust unit 7 + integration mysql/mariadb EXIT=0, tokens 0 mới. **STOP chờ user duyệt trước khi sang U3 (MSSQL — gồm driver fix is_raw_batch).**
