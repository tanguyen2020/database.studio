- **U4 — ClickHouse — ✅ DONE**. Pure builder `src/lib/users/clickhouse.ts` (createUser [auth sha256/no_password/plaintext + HOST + DEFAULT ROLE/DATABASE, escape `\'`/`\`/backtick `\``] / alterUserPassword / renameUser / dropUser; createRole/dropRole/grantRole/revokeRole/setDefaultRole; grant/revoke scope *.*|db.*|db.table; **§1.8.2 dbPreset** read-only/read-write/full/revoke-all — **UPDATE/DELETE = ALTER UPDATE/ALTER DELETE mutations**, không có EXECUTE) unit 5. Backend `users_admin.rs` CH views: users (name/storage/auth_type/default_*), roles, grants (system.grants), role_grants, can_manage (ACCESS MANAGEMENT probe) — Rust unit +1 (9 total, +clickhouse). Frontend: `ClickHouseUserManager.svelte` (toggle Users/Roles; **read-only badge cho storage≠local_directory** [users.xml] + banner + ẩn edit; tabs General [password/drop] · Grants [grid database×SELECT/INSERT/UPDATE/DELETE, cell ✓/☐ từ system.grants, preset R/RW/Full/Revoke, tooltip access_type thật] · Roles [grant/revoke/set-default]; banner thiếu ACCESS MANAGEMENT; Pending + Execute qua HTTP) + `ChCreateUserDialog.svelte` popup (mode user/role, auth dropdown + cảnh báo plaintext/no_password, Show/Generate, live preview) + store `chuser.svelte.ts` + mount App.svelte + dispatch. Demo: users_view **branch theo system (resolve từ connId)** — CH shape name/storage riêng, tránh đụng view-name dùng chung ('users'/'roles') với PG/MySQL. Tests: unit 5 (clickhouse.ts) + Rust 9 (users_admin); e2e `user-manager.spec.ts` +1 (CH users + users.xml read-only badge + Grants preset `GRANT SELECT ON \`public\`.* TO \`app\``) = 6 total; **integration `clickhouse_user_manager_end_to_end` EXIT=0 (env CLICKHOUSE_DEFAULT_ACCESS_MANAGEMENT=1) — 6 bước §1.9**: CREATE USER (verify storage='local_directory' — **khoá [CẦN XÁC MINH] §6.1.2**) → LOGIN → SELECT denied → CREATE ROLE + GRANT SELECT + GRANT role + SET DEFAULT ROLE → SELECT OK via role → INSERT denied → REVOKE → denied → DROP. CH count() trả string (UInt64) — assert "2"/"0". Gates: check 0/0, vitest 624, playwright user-manager 6/6, Rust unit 9 + integration clickhouse EXIT=0, tokens 0 mới. **STOP chờ user duyệt trước khi sang U5 (MongoDB).**
